# AGENTS.md

本仓库是"招聘自动化助手"(RecruitHelper):对旧产品"智联招聘自动化"(工作区 `../AutoZhilian`,五仓库)的全新重构,**不是延续开发**。定位是全新产品、全新安装:不背旧系统的任何二进制与协议契约——旧 machine_id 出生逻辑、端口 17321、`%APPDATA%\ai-assistant-console` 数据目录、electron-updater 更新链、Native Messaging host、旧扩展 ID,一概不保留。旧工作区的一切文档默认过时、仅作考古线索(见"文档信任边界与知识继承")。旧后台（xq-resume-backend）在客户交付前只允许为本仓库明确获批的用途改动，除此之外不动（2026-07-31 甲方裁决，原文为"仍不改动"；首个获批的改动用途是「全局约定·现场数据上报」，条款见该节）；2026-07-22 起获准的旧业务接口范围是：Go client 可先用后台激活码调用 `/api/v1/client/bind` 取得正式 `licenseToken`，再调用 `/api/v1/client/job-config` 与 `/api/v1/client/job-configs`，把旧后台严格当授权入口与职位配置面，将响应导入本仓库自有的不可变职位配置版本。兼容 `machineId` 按 [`docs/端到端业务主线计划-2026-07-22.md`](docs/端到端业务主线计划-2026-07-22.md) 从当前系统机器 UUID 派生，每次启动重新核对；它只作旧后台授权/配置面身份，不进入脑手协议或替代本仓库领域身份。激活码不得持久化，原始系统机器 UUID 不持久化、不上传、不进日志。`bind` 会创建或复用旧 binding 并产生旧后台审计，`job-config(s)` 会更新 `last_seen_at` 并产生 `client.verified` 审计，均为本裁决知情接受的服务端副作用。除「全局约定·现场数据上报」明确获批的上报接口外，不得因此接入 verify/heartbeat/session/task/metrics/alert 等其他控制面或上报接口；旧产品正在使用的既有接口、表与行为一律不得改动，`client_metrics`/`client_alerts`/`client_sessions`/`client_ai_model_logs` 等既有上报面仍不接入、不复用。Chrome 插件永远不连接旧后台。M5 起另一项获准云端出站是：Go client 直连一个 OpenAI 兼容 LLM provider；输入/输出 token 预算由客户端代码固定，provider 标签由 `base_url` 推导。`base_url`、API key 与 `model` 允许且仅允许来自已获准的旧后台 job-config 响应（三者在旧后台均为客户级配置，同一客户下所有职位与所有提示词共用同一值，不进入不可变职位配置版本，也不参与 `revisionHash`），落盘于同机 `llm-provider.json`（0600），生效于下次脑启动；后台未提供或值不合法时保留本机手工配置入口作为兜底。`model` 若为逗号分隔的多值，客户端只取首项，其余忽略；不实现失败换模型、降级或任何主备切换机制。客户端不再校验具体厂商与模型名，只校验非空与格式合法。插件永远不接触 key。上述 key 随旧后台响应下发，旧后台地址当前为 http，甲方 2026-07-30 知情接受明文传输风险，不为此收窄地址校验或拒绝下发。key 仍禁止进入普通日志、审计 detail、`brain.db`、管理 API、验收报告、AI 请求正文与任何远程渠道。2026-07-28 起第三项获准云端出站是运营通知 webhook，条款见「全局约定·运营通知 webhook」。2026-07-30 起第四项获准云端出站是客户端版本更新源，条款见「全局约定·客户端版本更新源」。2026-07-31 起第五项获准云端出站是现场数据上报，条款见「全局约定·现场数据上报」。除此之外仍视为不连云端。开篇"不背 electron-updater 更新链"禁止的是复用旧系统的更新源、旧包身份与旧 feed 契约，不禁止本产品自建更新机制；本仓库亦不引入 electron-updater 依赖（理由见 [`docs/自动更新能力规划-2026-07-30.md`](docs/自动更新能力规划-2026-07-30.md) 4.2）。

## 大方向(一切设计决策的根)

1. **脑手模型,主动权在脑。** client(Go 本地服务)是脑,Chrome 插件是手。旧系统是插件自循环、客户端被动应答(领待办、canRun 询问都由插件发起);新系统一切业务节奏(巡检、调度、重试、超时判定)只存在于脑,手只做两件事:执行脑下发的原语命令、上报传感事件。协议按本仓库 **`contract/协议规格-v1.md`** 实施(机器骨架 `contract/contract.v1.json`,推导底稿在 `docs/协议设计/`);旧的 `../AutoZhilian/脑手通信协议设计草案.md` 已退役、仅作历史参考——其"同 id 重发 3 次"等条款已被红队否决,不得照抄。注意区分两个方向:传输连接方向是手→脑(插件主动连 WS、断线重连、心跳),这是脑管理"不可靠的手"的方式;语义方向永远是脑→手。
2. **脑是平台无关的。** 命令语义一律意图级(如 openConversation(candidateRef)),DOM 结构、selector、tabId/windowId、平台专有 ID 不得进入协议语义,只允许出现在 result 的 evidence 与日志字段。验收判据:把智联换成任何别的招聘平台,client 端代码零改动,只改 plugin 的 program 层。
3. **测试与生产运行同一代码,构造性保证。** 插件端唯一入口是命令分发器;客户端测试页与未来的自动调度器都只是"命令生产者",共用同一信封、同一通道、同一分发路径。两端都禁止出现"测试模式"代码分支。

## 冒烟冲刺临时剖面(2026-07-22 甲方裁决)

[`docs/冒烟冲刺裁决-2026-07-22.md`](docs/冒烟冲刺裁决-2026-07-22.md) 是本剖面的原始授权记录。首个真实客户交付前，以最快速度跑通“采集 → 打分 → 自动招呼 → 完整沟通分支”为当前最高优先级，并临时覆盖与它冲突的旧里程碑停止点和验收程序：允许向真实候选人发送 effectful 消息，接受话术质量瑕疵；暂停有人值守逐条确认、批次制验收、转人工先行和 CC 前置审批，CC 改为每日事后审查。以下义务不暂停：

1. 所有发送仍走既有 WAL/idemKey/手证词/页面正证安全内核，不开旁路；每候选人每轮默认至多发送一条候选人可见动作。`docs/沟通逻辑规格-v4.md` 明确列出的 AI `话术_序列` 多气泡，以及其后可选的“正文→我方新发卡片”，是已批准例外：一个合法数组项只对应一个消息气泡，不按标点或项内换行再次切分；每个气泡和卡片必须拆成各自独立的业务动作事实、WAL、idemKey、证词和页面正证，严格按数组顺序逐项发送，最后一个气泡取得唯一正证后才可构造卡片。任一前项未确认、失败、suspect 或转人工时，所有后项均不得构造；已经正证的前项不回滚、不补发。不得把组合解释成任一原语内的多次动作。2026-07-26 甲方另批准候选人主动换微信请求的“接受→同轮 AI 承接”例外：只有接受原语取得唯一正证且联系方式收编事务完成后，才可创建一次承接建议；接受未确认、失败、suspect 或收编失败时不得调用承接 AI 或创建正文动作。承接失败不回滚接受事实、不再次点击接受；承接产生的多气泡仍逐项走各自独立安全轨。
2. 2026-07-22 后续裁决明确撤销“自动招呼每日 30 条上限”和“平台风控信号触发全局 effectful 停派”两项新增机制；不为本冲刺另建配额闸或全局风控熔断。平台原语按真实页面返回失败、既有守卫与人工停止能力不因此删除。契约仍以 `contract/` 为单一源并同步 codegen；任何仓库文档仍不得出现候选人 PII。
3. 已批准启用拒绝正则族、RSM-01/02 与 SR-01/02/04，优先级为简历事件 > 拒绝正则族 > 短语族；177/259/313 只可在各自就地只读探针确认后实现，343 继续走保守分支。
4. 本剖面只暂停人工/批次审批，不暂停增量提交纪律、规范先行、业务事实禁止物理删除与上述安全内核。首个真实客户交付前必须显式退出本剖面并恢复原纪律。

## 工程布局(monorepo)

| 目录 | 内容 |
|---|---|
| `contract/` | 协议契约单一源头:《协议规格-v1.md》(规范文本)+ `contract.v1.json`(codegen 输入,生成 Go 类型与 TS 常量)。协议改动只改这里,禁止两端各自手写对齐 |
| `client/` | Electron 外壳(窗口 + 启停 Go 服务)+ React 最小 UI(状态页、测试页)+ Go 本地服务(逻辑中枢:WS 服务、手注册表、命令派发、GORM + SQLite) |
| `plugin/` | Chrome MV3 插件。base = 稳定壳(manifest、监听注册、WS 连接、心跳/重连、命令分发器);program = 业务模块(原语注册表,普通 TS 模块) |

技术选型:Go 侧 SQLite 用 GORM + glebarez/sqlite(纯 Go 驱动,避免 cgo 拖累 Windows 交叉编译);UI 沿用 React + TypeScript(strict)。本地服务端口新定、可配置,不复用 17321。

## 插件交付与更新(2026-07-25 甲方裁决)

插件交付机制已经拍板，不再悬置：**不使用远程 JavaScript、CRX、自托管更新源或 Chrome Web Store；客户端整包携带 Chrome MV3 插件，首次由我方人工远程协助，以开发者模式从固定本地目录加载 unpacked 插件。后续更新先在 Chrome 不会读取的独立暂存目录准备完整新版，进入安全窗口后再原子替换固定正式目录，并紧接既有 `debug.reload@1` 维护握手激活。** 不开发面向客户的安装向导；远程安装成本显著上升时才重新立项。完整决策、当前能力边界与首客前义务见 [`docs/插件交付与更新决策-2026-07-25.md`](docs/插件交付与更新决策-2026-07-25.md)。

客户端更新插件时，暂存新版与“替换正式目录→重载→确认”是两个阶段。后者会换代插件并刷新已打开的平台页，因此从正式批次第一次读取推荐窗口起，到该批最后一个计划招呼终局前，既不得改写正式目录，也不得派发重载；若被业务阶段或未收束命令阻挡，只记录延迟并在安全边界合理重试，不得提前失效推荐流。一旦 `debug.reload` 已派发，仍严格遵守协议规格 §14.1：超时或歧义不得由通用重试链自动重派。

## 手的禁令(插件端纪律,骨架期即立法)

1. **不得有业务定时器。** setInterval / chrome.alarms 只允许基础设施用途:WS 重连兜底、心跳保活。什么时候干活由脑决定,感知类工作也以脑下发 readonly 对账命令为主。
2. **不得持久化业务状态。** chrome.storage 只放基础设施数据(连接配置、能力缓存)。手的失忆是设计前提,不是要修的 bug。(远期例外:协议规格 §9 的投递层证词 journal/outbox——四道栅栏下的基础设施数据、允许整体丢失、不构成第二账本,随首个真实副作用原语批次引入。)
3. **一切动作由 cmd 触发。** 传感事件(新消息、页面跳转、登录态变化)只上报不决策;事件是提示不是账本,丢了由脑的对账轮兜底。
4. **手不自作主张重试。** effectful 动作失败就诚实上报 result(带 sideEffectPossible),重试与验证策略全在脑。
5. **监听注册只在 base。** program 不注册任何 chrome 监听、只经原语注册表暴露能力；客户端整包更新插件时，base/program 仍作为一个可验证构建整体交付，不形成第二套监听或更新入口。

## 原语契约(手的能力怎么登记)

每个原语按协议草案 §9 注册:`name、argsSchema、class(readonly|effectful)、preconditions、evidenceSchema(effectful 必有)`。调试用原语归 `debug.*` 命名空间,不占用平台无关词汇表。effectful 原语的完成信号是"观察到后置条件"(evidence),"我点了按钮"不算完成。

## 防护成本预算(守卫与机制立法,2026-07-20 甲方裁决)

"宁可少发"仍是红线,但少发方向的损耗——假阳性拦截、守卫脆断返工、人工票据——自本章起是有预算的成本,不是免费的美德。安全内核、外围守卫与新增机制分层立法:

1. **内核冻结。** 脑账本闸(intent/idemKey 唯一)、手证词闸(attempting→committed 双写)、平台回读闸(guards 条件写+发后页面回读)、原语内不重试、成功=发后页面可见(2026-07-29 甲方裁决,升格条款见本章第 9 条)。任何"简化"指令均不适用于内核;拿不准某物是否属于内核,先问甲方,不许自行裁剪。
2. **新增守卫立法三问,缺一不收:**
   - **场景推导**:写出具体事件序列,证明缺这道守卫会导致多发/错靶/覆盖用户输入。"更稳妥""多一层保险"不是场景(`is-active` 事故的直接立法)。
   - **边际价值**:说明现有守卫链为何覆盖不了该场景。
   - **脆性定价**:依赖平台公开行为(URL、服务端 ID、可见文本)=低脆;依赖平台私有内部(框架内部字段、组件树、样式类名)=高脆。高脆守卫默认只准进观测模式(采进 evidence/日志,不拦截),转正为硬闸需真机假阳性数据+甲方裁决。
3. **本章约束的是守卫(拦截判断),不约束感知数据通道。** 读取页面状态是感知的固有成本,通道选型以真机证据与最低平台侵入为准;通道失效的方向必须是"读不到→不确认→转人工",不得多发。
4. **红队产出分级,不自动全收。** 按"可达性×后果×现有闸覆盖"分:阻断级(可构造出多发/错靶,必修)/观测级(理论可达,先观测)/记录级(显式接受归档)。红队只有提出权,观测级以上由甲方裁决;修复优先砍能力(如取消自动切会话),其次才是加机制。
5. **运行模式分层。** "有人值守"与"无人值守"是两个验收剖面。首个真实动作、新平台、新原语先过有人值守剖面(内核硬性、外围观测);无人值守转正=足量真实成功+外围假阳性数据。人在场时,人算一道闸。
6. **契约是永久负债。** 向 append-only 契约加枚举/字段/原语前,先回答"evidence 文本或日志是否足够";诊断辅助默认不进契约。
7. **拿不准时提交裁决,不许默认加码。** "拿不准=effectful"只约束副作用分类,不授权守卫通胀。出现"再加一道闸 vs 成本"的取舍,写清三问交甲方选,默认进观测模式。
8. **裁决旋钮=人工介入率。** 每百次发送的人工介入次数与守卫假阳性率是外围守卫的存废判据,验收报告必须记录;守卫去留以此数据裁决,不以推演强度裁决。
9. **守卫只核对世界状态与动作接口的公开语义,不审计平台实现(信任边界条款,2026-07-20 经执行方质询修订)。** 动作前守卫的合法对象有二:一是"世界是否处于脑预期的状态"(路由、可见内容与控件唯一性、消息指纹、账号身份、用户输入);二是**被直接调用的动作接口的公开、标准语义**(如按钮的 `type`/`disabled` 等标准 DOM 属性)。平台私有层之间的相互一致性——前端框架内部对象(组件树/渲染节点/事件注册表,如 Vue owner/VNode/listener)、页面 SDK、渲染层与事件层是否接对线——不得作为动作授权,也不适用观测模式降级:对实现的不信任没有自然边界,证完事件处理还可证 SDK、证完 SDK 还可证服务器。判据看守卫**问的问题**,不看它读哪里的数据(数据源脆性另由本章第 2 条管)。
   本条同时冻结系统的保证边界:**构造性保证的对象是"自己的逻辑对不可逆动作至多调用一次";"平台对一次标准 click 至多产生一次副作用、且作用于界面所示对象"是被信任的平台动作契约,不是待证命题。** 不愿接受该信任时,出路是改用更可靠的公开接口、削减自动化能力或转人工,不是向平台内部追证。后置条件核验、配对验证读与 suspect 负责判定动作结果与收敛歧义;它们能捕获并升级已发生的伤害,**不能补救**——因此凡能在有限、低脆的公开边界内预防多发/错靶/覆盖输入的守卫,仍按三问立案,不因"事后能发现"而免立。
   **可见后置状态原则(2026-07-22 甲方裁决)。** 对页面副作用动作,事先定义且与本次目标明确绑定的可见业务状态变化,可以作为成功正证;例如同一候选人的“打招呼”变为“继续沟通”。动作后页面无变化、目标消失或状态读取失败,只能记为“结果未确认”,不得据此推断副作用未发生,也不得自动重试或另铸同义意图。只有平台明确呈现的可信失败结果,才能判定失败。
   **成功=发后页面可见(2026-07-29 甲方裁决,替代"成功=服务端可见",系可见后置状态原则升格为总则)。** effectful 消息动作派发后,回读会话最近窗口,读到一条方向、内容与本次动作一致、无失败标志、时间不早于本次派发的消息,即判成功并收编;同文歧义按"满足条件的最新一条即本次"处理。发后核验不再要求历史指纹锚对齐、深分页或历史到顶判定,验证读改为首轮+短暂间隔复读一轮。读不到→不确认、不自动重发、转人工。命令返回值与"点击已完成"仍不构成成功。WAL/idemKey/手证词/原语内不重试原样保留;动作前的 guards 条件写(expectedTail 基线、composer.empty、身份与目标绑定)不属于本裁决、原样保留。完整裁决与拆除/保留清单见 [docs/发后核验成功判据裁决-2026-07-29.md](docs/发后核验成功判据裁决-2026-07-29.md)。
10. **新增跨层机制立法四问,缺一不收。** 本条只约束新增或扩张的跨层、持久化、自动重试、恢复、迁移、兼容与通用化机制,不授权削弱本章第 1 条已经冻结的安全内核。实现前必须同时写明:(一)当前范围内可达的具体事件序列及其实际损失,不得用假想未来用户或“以后可能需要”代替;(二)现有机制、直接实现、削减能力或人工兜底为何不能覆盖;(三)方案是否以最小状态和最窄边界完整闭合该故障链,而非只把歧义转移到下一层;(四)新增状态、持久化数据、迁移/恢复路径、协议表面、测试矩阵、误伤率、维护成本与延迟真实反馈的总代价是否与预期损失相称。没有当前证据时直接解决当前用例,不先造通用框架;单一用例若已有不可逆损失证据或已批准的近期硬义务,仍可立案,不得把用例数量本身当作机械门槛。

## 增量交付、提交与停止纪律(2026-07-20 甲方裁决)

本章纠正“闭门追求最终正确、攒成大提交、用无限审查替代真实反馈”的工作方式。目标是让进度持续可见、每一步可回退、风险由主代理主动裁决。它不削弱已经冻结的安全内核,但禁止以安全之名无限扩张外围机制与审查范围。

1. **战役级工作开工先冻结出口与预算,原子级工作豁免(2026-08-02 甲方裁决分层)。** 战役级=预计不止一个 commit、或需修改规格/契约、或触碰安全内核外沿、或改变候选人可见行为语义的实现任务:动代码前主代理必须在工作更新或计划中明确四项——本轮最小可交付出口、必须立即阻断的损害、允许重试/人工兜底的失败、独立审查轮次与明确后置项——并等甲方看过后的明确同意才动手。原子级=单 commit 可收尾、不改规格、不碰安全内核、不碰存量冻结数据的小改:向甲方说明意图后即可动手,不等待确认,完成即报,甲方随时可叫停;中途膨胀成战役级时立即停下补出口四项;拿不准属哪级,按战役级处理。没有写进出口的发现不得静默扩成本轮范围;若会实质改变范围,先交甲方裁决。
2. **最窄竖切冒烟一绿,立即提交。** 第一条真实生产路径以最小实现跑通并通过与风险相称的冒烟测试后,必须马上创建本地 commit,禁止等待后续优化、全量审查或完整真机验收后再做首个提交。commit 是工程检查点,不是“全部完成”的奖章。
3. **后续一项因果变化一个 commit。** 每个被采纳的独立缺陷修复、可靠性优化或重构,连同对应回归测试/文档/生成产物,验证后单独提交;进入下一独立项前应恢复干净工作区。不得把多轮审查意见攒成一个大提交,也不得把无因果关系的顺手修改混入。契约源与同次 codegen 产物必须留在同一 commit。
4. **提交默认授权仅限本地历史。** 甲方要求实现或修复,即视为授权按本章在当前分支创建上述本地 commits,无需每次重复询问;甲方明确说“先别提交”时例外。不得擅自 push、amend、rebase 或 squash;整理/改写历史须另获甲方指令。任何 commit 都必须对其声明范围自洽,不得为追求数量故意提交已知不可构建状态。分支或 worktree 合回 `main` 一律使用 `--no-ff`(2026-07-29 甲方裁决),**即便当时 fast-forward 可行**:合并节点是整批工作的回退把手(`git revert -m 1`)与批次边界(`git log --first-parent`),并承载“整批范围与未验证项”这类不属于任何单个 commit 的说明;ff 会让一批工作在 main 上散开并与其他会话的提交交错,这三样随即失去。能 ff 不等于该 ff。
5. **审查有预算,发现不等于授权。** 冒烟 commit 后默认最多一轮独立审查;修复阻断项后只做该 diff 的定向复核,不得自动重开全量审查。审查/子代理每条发现必须给出当前可达事件序列、实际后果、现有覆盖、发生概率与修复成本;“理论上还能更稳”不成立。子代理只有举证权,主代理保留裁决权,不得让审查流替代产品判断或递归派生新审查。
6. **全局发现按预期损失分流。** 阻断级=当前范围存在可达路径,会造成重复/错靶外部副作用、覆盖未发送输入、不可恢复的数据/账本破坏、错版仍发副作用,或需要重装/手工清存储才能恢复的长期锁死;此类自动阻塞并立即修。优化级=常见且边际价值明确、修复成本低;在预算内以独立 commit 处理。记录级=低概率、可恢复、有人兜底或纯理论反例;登记后不阻塞。判据是“发生概率×实际损害”相对修复成本与真机反馈延迟,不是能否继续构造反例。
7. **阶段风险预算不得被执行方擅自升级。** 当前“首客前、开发者模式、本地通信、有人值守”阶段,默认接受绝大多数情况下可靠并由真人兜底的可恢复边缘失败;人在场本身算一道闸。不得把无人值守生产 SLA、对外兼容负担或零风险标准偷渡进开发期工具。进入真实客户或无人值守阶段时,另行显式升级出口和风险预算。
8. **真实反馈优先于静态完美。** 最小路径已经可真机验证时,除阻断级问题外,不得以继续推演、补外围守卫或“最后再审一轮”为由推迟真机。真机观察到的新事实可开启下一独立优化与 commit;未观察到的边缘猜想默认进记录级。
9. **停止条件具有约束力。** “冒烟已绿且已提交 + 当前可达阻断项为零 + 本轮要求的真机验收通过”即为完成出口。达到后主代理必须停止扩题并交付结果;继续优化需要真实失败证据、原计划中的明确后置项或甲方新授权。不得因为仍能找到 P2/P3 就宣布任务尚未完成。
10. **主代理必须主动踩刹车。** 风险分级、范围控制、提交时点和停止决定都是主代理职责,不能等甲方发现节奏失控后充当外部节流阀。收到子代理意见或临时反例时,先裁决价值再行动;不得先全收全修,再以口头承诺“以后不过度”补救。
11. **既有大脏批次只做诚实补救。** 本章生效时若工作区已积累大量未提交改动,按最小可构建、可解释的因果单元拆分,逐个验证并提交;若依赖关系使某个更小中间态无法自洽,就合并成最小可构建单元。不得伪造并未发生的测试时点或为了制造漂亮历史提交破损快照。真正的改进是从下一项起实时提交,不是事后表演增量。
12. **规范不得在同一批次中自我授权。** 若实现需要对现有可信规范作规范性修改——包括扩大范围、放宽或改变约束、增加安全保证,或新增持久化、自动恢复、迁移、兼容义务——执行方必须先冻结相关实现,向甲方并列提交旧约束、拟议文本、当前可达场景、收益与成本,获批后再按“规范先行→实现与生成产物同步”的顺序落地。同批新改的规范不得被引用为该实现已有授权或独立正确性证据。已获批裁决的如实固化,以及不改变权利义务、风险预算和运行语义的勘误或事实同步,不属于自我授权,可按正常规格先行纪律执行。
13. **多会话共用一个仓库,主检出是发布台不是工地(2026-07-29 甲方裁决,2026-07-30 修订适用粒度与合并核对)。** 本仓库常有多个会话并行推进,而主检出的 HEAD、当前分支与工作区是所有会话共享的可变状态——任何"我记得 `main` 是 X"的假设随时失效,且失效时不会有任何报错。因此凡是"一段工作"——预期不止一个 commit、需要跑门禁迭代验证、或会在两次交互之间留下未提交中间态——一律在各自的 worktree 内进行,开工第一件事把基线对齐到本地 `main`(新建 worktree 可能基于落后的 `origin/main`);允许直接在主检出完成的只有当场一个 commit 收尾的原子小改(勘误、单点小修),条件是动手前核对主检出正在 `main` 且工作区干净(有他人残留即停)、完成立即提交、离开时恢复干净,中途膨胀成第二个 commit 或需要迭代调试时立即迁去 worktree 续做;拿不准按"一段工作"处理。此类单 commit 直接落在 `main` 上,自成回退单元,不适用第 4 条合并节点要求。主检出此外只用于合并与打包,不在其中建功能分支;收工前先在自己的 worktree 里 `git merge main` 解冲突并跑门禁,再回主检出做一次 `--no-ff` 合并、随即返回,在主检出的停留应是一次操作而非一段工作。回主检出执行合并前,先核对主检出正在 `main` 且工作区干净,异常即停;合并前必须先跑 `git log --oneline main..HEAD` 核对将要带入的提交,除本轮自己的提交与自己刚做的"merge main"合并节点外不应出现任何别的提交,出现陌生提交即说明分支基线已被他人移动,此时停工交甲方裁决,不得自行 reset、rebase 或挑拣提交。`--no-ff` 合并若出现冲突,说明核对后 `main` 又被他人推进,应 `git merge --abort` 回 worktree 重新 merge main 并重跑门禁,不得在主检出现场解冲突。打包同样在主检出进行,前后各记一次 HEAD,不一致即作废产物并停工。

## 悬置决策(有截止点,不许默默溜过)

- **契约 append-only 冻结点**(2026-07-20 甲方裁决,同日经执行方修正):首个真实客户安装前,contract 不受 append-only 约束,允许破坏性修订(删值、更名、收窄)。本许可只针对**对外兼容形态**;安全内核语义(证词、幂等、验证、suspect 结构)不因此获得削弱许可。条件:每项删除须证明无生产者、无消费者、无持久化/恢复路径引用、非明确未来占位,存疑项列保留清单,不凭源码搜不到引用即删;同一 commit 内重生成两端产物并通过全量门禁,规格文本与机器契约同步修订(must-ignore 接收纪律不随本条豁免);已持久化的历史记录按**派发时契约**解释、视为不可变审计快照,只有仍需参与运行且不兼容的才清理并在 commit 记录;破坏性修订按硬切换生效:暂停派发→重启脑→重载插件→确认 contractHash 一致后才恢复命令,mismatch 状态禁发 effectful。**截止点 = 第一次给真实客户装插件之时**,发生时以 commit/tag/日期记录冻结事件;此后恢复 append-only 纪律,未清理化石在当前 proto 主版本内保留,再清理只能经新原语版本或主版本迁移。
- **表结构**:当前只建骨架所需最少表,全部视为临时;正式设计时候选人主键必须带 platform 维度、锚平台 userId(不锚 resumeNumber——旧系统教训)。
- **授权/云端对接**:激活码 `bind` 与 `job-config(s)` 配置面读取已由 2026-07-22 裁决提前；verify、heartbeat、session 与其他控制/上报接口仍后置。届时后台不改，另按现有 `/api/v1/client/*` 冻结契约立案对接。

## 文档信任边界与知识继承

- **唯一信任源是本仓库的文档**:AGENTS.md(宪法)、`contract/协议规格-v1.md`(协议)、`docs/沟通逻辑规格-v4.md`(沟通行为目标规格,已自旧工作区收编,以本仓库副本为准)、`docs/协议设计/`(推导底稿)。文档冲突时按 AGENTS.md > 协议规格 > 其余 裁决。
- **旧工作区(`../AutoZhilian`)的一切文档默认过时,不作依据**:只当考古线索。从中取用任何事实(页面结构、接口行为、平台参数)前必须真机验证;经验证的结论固化进本仓库文档后方可引用。新系统真正依赖的旧文档,应收编进本仓库成为正式副本,不跨目录引用。
- **代码级对抗知识照旧是搬运素材,但同样不盲信**:可以主动参考旧项目实现各种功能的方式,但它们只进入候选方案池;必须结合新架构约束、当前真实页面和自动化证据独立判断,只有确实更好的部分才迁入。旧 program 约 1 万行 DOM 页面对抗代码与 `../AutoZhilian/html定位/` DOM 样本,搬运时以真实页面为准逐条核对(重写不等于重踩坑,但旧注释与旧文档不是证据)。
- **平台枚举面事实门(2026-07-22 甲方裁决)。** 凡新建平台枚举面(消息类型、卡片类型、页面状态等),事实门必须先考古老项目同一枚举的已知值全集作为候选清单,并逐条标注“真机已见/真机未见”;未见值不得实现。未见值归并到保守分支时,仍必须把原始类型保留进 evidence 或普通日志,禁止静默丢弃;考古候选只决定要验证什么,不替代本仓库的真机事实。
- **页面事实发现顺序(2026-07-24 甲方裁决)。** 对可见 DOM、标准属性、控件状态及可逆交互,默认先使用 GPT Chrome 在当前真实登录页面完成只读考古,不得为普通页面定位优先新增插件探针。事实发现不能替代生产验收;正式能力仍须通过 RecruitHelper 的真实脑手路径验证。只有关键事实无法由 GPT Chrome 获取,或需要证明 MAIN/isolated world、扩展权限及正式执行环境差异时,才允许加入最小只读临时探针,并须说明必要性、用完删除,不得残留为 capability 或契约表面。
- 几条已固化进协议规格的红线(此处仅提醒):宁可少发,核验失败不自动重发、只转人工;AI 是建议者不是执行者,状态推进由确定性代码裁决;沉默锚点只算真正已发出的消息。

## 全局约定

- 界面文案与交流全部中文;源码不使用 emoji。
- **统一业务运行窗口（2026-07-25 甲方裁决，2026-07-28 修订 24:00 边界粒度）。** 正式业务工作流只允许在客户端本地时间 `[08:00, 24:00)` 由用户显式开始或恢复；该窗口同时约束完整流程、仅多轮回复、采集、评分、筛选、招呼语生成、候选确认发送和沟通巡检，不存在“凌晨只跑漏斗”的例外。`[00:00, 08:00)` 产品 UI 仍可打开和只读查看，但开始、恢复和候选人可见新链均不可用，也不得登记预约后到点自动开跑。运行跨过 24:00 时，不得开始下一候选人、下一阶段，也不得为新候选人或新 turn 铸造 effect intent；已经开始的单个 AI 调用或手侧命令仍按既有账本/恢复纪律自然收束。按 [`docs/24点边界裁决-2026-07-28.md`](docs/24点边界裁决-2026-07-28.md)：当前候选人的回复动作链若在 24:00 前已发出首条候选人可见动作，其余下气泡与已获正证前提的卡片按既有节奏与安全轨继续收束到终局，视为当日运行的自然延续，接受数十秒级跨点尾巴；尚未发出任何可见动作的 turn 不跨点，整体悬置至次日。收束期间链内失败、断连、suspect 或转人工即止，不为收束新增重试；残留 planned 动作仍走既有恢复轨。既有 effectful WAL 的判定与收敛不算新业务授权，仍须继续。沟通阶段的运行在链终局后按跨日终局处理（`completed` + `dailyWindowClosed`），漏斗阶段跨日持久化为 `waitingDailyWindow`；两者次日均须用户显式开始/恢复，08:00 到达本身不授权。自动化测试可使用注入时钟覆盖窗口边界，开发者只读诊断与维护不冒充正式业务运行。
- **开发期业务窗口覆盖（2026-07-27 甲方裁决）。** 仅在已授权测试账号、有人值守的本地真机验收中，允许脑进程启动时以精确环境变量 `RECRUITHELPER_DEV_ALLOW_OUT_OF_WINDOW=1` 把民用时间窗口视为全天开放；变量缺失、空值或任何其他值均保持正式 `[08:00,24:00)` 规则。该覆盖只改变“当前时刻能否开始/恢复/进入下一业务成员”的裁决，不伪造系统时间，不改变消息、业务事实、审计、deadline 或本地日期，也不自动创建、开始或恢复任何工作流。用户显式开始/恢复、暂停与成员边界、账号/身份、manualRequired、contractMatch、WAL/idemKey、手证词、平台正证及所有 effectful 安全闸仍原样执行。开关只在脑启动时读取，不持久化，不经产品 UI、管理 API、旧后台或插件配置写入；UI 只读取脑投影的窗口结论，不另行按渲染进程时钟授权。正式客户启动脚本与安装包不得设置此变量；恢复正式规则必须完整退出客户端并在不带变量的环境中重启，首个真实客户交付前须复核本开发入口是否继续保留。
- **平台交互节奏与条件等待(2026-07-23 甲方裁决)。** 相邻的平台可见交互动作(点击、输入、滚动、打开/关闭、切换等)必须至少间隔 1 秒，并在适用下限之上加入小幅、有界的随机抖动；业务链另有更高下限时取更高值。批量处理相邻候选人的随机等待下限为 2 秒，当前默认范围 4～8 秒（2026-07-27 甲方调慢，同批交互抖动默认 2.5～5 秒）；打开候选人简历后必须至少停留 2 秒才允许关闭。等待页面元素或状态就绪时必须使用条件轮询：条件满足即继续，最长等待 10 秒，不得替换为固定等待 10 秒；超时只表示本轮未就绪，不能据此反推副作用失败或授权重做可能产生副作用的动作。条件就绪/稳定等待不能被交互节奏下限取代。
- **推荐页运行连续性(2026-07-23 甲方真机事实，2026-07-25 补充)。** 智联推荐页刷新后会换一批候选人，刷新前的候选人不能假设仍可通过稳定平台用户 ID 在新推荐流中找回。客户端正式 `debug.reload` 维护路径会在插件换代后刷新已打开的平台页，因此从正式批次第一次读取推荐窗口起，到该批最后一个计划招呼终局前，禁止激活插件更新或刷新推荐页；若故障修复迫使中断，已由 WAL 终局的发送照常保留，未绑定发送意图的旧推荐目标不得继续定位或发送，须显式结束本次运行并重新采集。单独在 Chrome 管理页手动重载插件不必然刷新页面，但当前脑仍会把 `bootId` 换代保守视为推荐流失效；甲方接受这一低频假阳性，暂不为它新增区分机制。稳定 ID 只负责同一运行现场内的身份与去重，不等于平台提供跨刷新搜索能力。
- TypeScript 开 strict;协议类型一律来自 contract codegen,禁止手写重复定义。
- **AI provider 数据边界（2026-07-21 甲方裁决，2026-07-23 增补本地追踪与微信资产例外）。** AI provider 请求只携带完成当前建议所需的职位上下文、简历正文、消息角色/正文与状态事实，不携带 platformUserRef、accountRef、conversationRef、profileId、resumeNumber、候选人 displayName 或结构化微信号。简历与候选人自由文本可能自含姓名、电话等身份，甲方知情授权其作为当前模型输入，系统不承诺内容级脱敏。普通日志、审计 detail、`brain.db` 的诊断面、管理 API、验收报告和未来上报只允许 capture/snapshot/invocation/turn/action 随机引用、用途、provider/model、配置 hash、输入输出 hash、字节/token 数、延迟、状态、错误分类与估算费用；禁止记录 API key、base request/response、聊天正文、简历正文、完整 prompt 或候选人明文身份。唯一 AI 原文例外是同机数据目录内独立的 `ai-traces.db`：它是本地 AI 实验语料库，不是普通日志或业务恢复账本，按 [`docs/AI调用追踪设计-v1.md`](docs/AI调用追踪设计-v1.md) 为每次调用保存去除鉴权材料后的完整 provider 请求与原始响应，并以 `invocationId` 与 `brain.db` 的无正文诊断摘要关联。任何层均不得保存 API key、`Authorization` 或其他鉴权 header；`ai-traces.db` 原文不得经普通日志、`brain.db`、管理 API、验收报告或任何远程渠道暴露。追踪库写入失败不得阻断或改变原 AI 调用和业务裁决，但必须在 `brain.db` 留下无正文 `traceStatus` 并向 stdout 响亮报告；若连 `brain.db` 也不可写，至少向 stdout 报告。首个真实客户交付前必须重新裁决原文保留期限、静态加密与用户开关，裁决完成前不得把当前开发期默认直接带入客户环境。业务数据库为完成重放而持久化的消息、简历快照、建议正文与权威联系资产不属于“日志/上报”；其中候选人微信号只允许存在于获准原语的 result、手侧既有 journal/outbox 生命周期、不可变 `CmdRecord.ResultBody` 审计快照、权威 `ContactAsset` 事实和运营通知 webhook 正文（2026-07-28 裁决，条款见「全局约定·运营通知 webhook」），不得进入 evidence、错误详情、stdout、普通日志、审计 detail、管理 API/UI、报告或 AI 请求，也不得返回招聘方自己的微信号（本句对 `/admin/*` 诊断面的部分已被 2026-07-31「开发者诊断台明文边界」整体解除，其余去处不变）。脑成功收编后以 `ContactAsset` 为业务权威，不从命令审计快照反向重建资产。上述业务事实仍受本地数据边界与禁止物理删除约束；滚动日志、支持包导出、追踪清理与查看 UI 均后置，不属于本次授权。
- **运营通知 webhook（2026-07-28 甲方裁决）。** 第三项获准云端出站：Go client 向代码内写死的唯一企业微信群机器人 webhook（我方运营统一群，URL 由甲方指定并裁决写死、不设配置面，不经插件或旧后台下发）发送运营通知，正文携带客户名以区分来源。触发仅限两类业务事实：换微信成功（权威 `ContactAsset` 收编完成）与约面成功（主线真迁入已约面），每候选人每类事件按 event_key 终身至多通知一次。载荷仅限：候选人 displayName、所属职位名、客户名、面试时间、主线/微信线状态文案、该候选人微信号（取自权威 `ContactAsset`）、该候选人的聊天记录/简历截图 JPEG，及**候选人画像摘要**（2026-07-28 甲方补充裁决：仅年龄、最高学历、现居地、期望薪资四项，逐项取自本机简历快照的对应标签，缺项省略、整行可缺，不得改写为其他简历字段，也不得携带简历正文、工作经历、自我评价或联系方式类内容）。通知走本地发件箱（event_key 幂等、重试上限、超期过期标记；发件箱行只存事件元数据与职位名，不存候选人姓名/微信号/正文，正文按发送时刻现算）；发送与截图任何失败只降级（缺图 15 分钟兜底发纯文本、失败留表可查），不阻塞或改变业务主线裁决；webhook 发送不属于候选人可见动作，不受统一业务运行窗口约束。该渠道不得复用于任何其他上报或控制面；webhook URL 不进入日志、审计、管理 API 与上报。截图 JPEG 是本地业务证据资产（blob 内容寻址存储 + 截图事实行，受禁止物理 DELETE 约束），不进入普通日志、审计 detail、管理 API、验收报告或 AI 请求，除本 webhook 外不得出本机。
- **客户端版本更新源（2026-07-30 甲方裁决）。** 第四项获准云端出站：Go client 向我方自建的**只读静态**更新源发起匿名 GET，取版本清单（版本号、下载地址、sha256、大小）与安装包本体。更新源地址写死在客户端代码或同机配置，不经旧后台、插件或 AI 下发；旧后台不参与更新分发。请求不携带业务数据、候选人信息、密钥或身份凭据，查询参数只允许当前版本号与客户标识，后者仅用于灰度分组。响应只被用于"有没有新版、从哪下载、下载物是否完整"三件事，任何字段都不得成为业务裁决、配置面或控制指令的来源。安装包落盘后必须经 sha256 校验通过才可执行，校验失败即丢弃并记录，不得执行未校验的产物。该通道单向只读，不得复用于任何上报、遥测、控制面或配置下发；更新源地址与客户标识不进入 AI 请求、运营通知 webhook 与旧后台。**是否安装、何时安装由脑裁决**：安装会中断业务并重启脑，因此只在没有活跃工作流、没有未收束命令时进行；检查与下载不是候选人可见动作，不受统一业务运行窗口约束，安装本身另按 [`docs/自动更新能力规划-2026-07-30.md`](docs/自动更新能力规划-2026-07-30.md) 4.3 择时。插件仍随客户端整包携带，本条不授权任何独立的插件下发通道。
- **现场数据上报（2026-07-31 甲方裁决）。** 第五项获准云端出站：Go client 向旧后台新增的现场数据上报接口 POST 上传一个由脑现场打包的诊断包，鉴权复用已获准的 `licenseToken`，客户身份由服务端从 token 解析，客户端不另行自报客户名。**触发方式限两种（2026-07-31 补充裁决，原文为"只能由同机诊断台的人显式点击触发，不得定时"）：同机诊断台的人显式点击，以及在诊断台显式开启「每日自动上传」后由脑在客户端本地时间每日 00:10 自动触发**。除此之外不得由业务事件触发；两种触发都不得自动重试。自动触发前必须确认没有活跃工作流、没有未收束命令，否则顺延，当日 02:00 仍不满足即放弃；客户端未运行而错过的当日不补传。**开关默认关闭，且只有人在 `/admin/*` 诊断台上显式点击这一条路径能把它打开**——安装、升级、迁移、重装、配置下发与任何默认值填充都不得使其变为开启，产品 UI 不暴露、不投影该开关。服务端必须按客户实施留存上限（保留最近 14 天），超期包删除文件并在元数据行标记已清理；每日自动上传若不设上限，十客户规模下一年即耗尽当前服务器磁盘，故留存清理必须与自动上传同批上线。自动上传的成败必须在诊断台可见，失败时同时向 stdout 响亮报告；不得为此新增任何外发通知渠道，运营通知 webhook 尤其不得复用。自动上传使候选人明文的外传不再有人在场知情，本条后述"首个真实客户交付前必须重新裁决……是否需要客户知情或授权"因此升为必答项，不得把开发期默认带入客户环境。包内容是**白名单**：`brain.log` 与 `brain.log.old`、`brain.db` 的一致性快照、`ai-traces.db` 的一致性快照，外加一份含客户端版本、打包时刻、各文件字节数的清单。除此之外一律不含，代码层硬排除 `llm-provider.json`、`legacy-job-config.json`、`*.bak*` 与 `blobs/`；API key 在任何情况下不得进入诊断包。本条据此把「AI provider 数据边界」中 `ai-traces.db` 原文的「任何远程渠道」禁令、以及候选人明文「收在同机」的边界，**仅就这一条上报-留存-取回链路**开例外；这两条对其他一切代码路径的约束不变，不得援引本条在别处放宽。包**不加密、经明文 http 上传并在服务器上明文留存**，甲方 2026-07-31 知情接受该暴露（候选人姓名、微信号、聊天正文、简历正文），不为此收窄上报范围。取回有且只有两条路径：服务器 SSH，以及管理前台（console-admin-web）的审计页经 `/api/v1/admin` 下载，后者必须走既有 admin token 鉴权、包路径不可枚举、**不得以任何形式挂成 nginx 静态目录**。服务端落盘目录必须位于 web 服务器可读根之外（不得置于 `/srv/console/` 前缀下），该约束由部署后的外网实测验证，不得仅凭配置推定。该通道只上行、不下行：上传响应只用于告诉本机"传成功没有"，任何字段都不得成为业务裁决、配置或控制指令的来源；诊断包与其元数据只服务我方排障，不得回流为旧后台对客户端的任何控制面或配置面。上报不是候选人可见动作，不受统一业务运行窗口约束。它是开发期工具，首个真实客户交付前必须重新裁决是否保留、服务器侧留存期限与清理、以及是否需要客户知情或授权。
- **同机产品 UI 业务投影例外（2026-07-25 甲方裁决）。** 为普通用户七页客户端新增独立的 `/app/*` 产品 API；它不是 `/admin/*` 诊断/管理面，也不是脑手 contract。该 API 只监听 loopback、只接受 Electron preload 内存中的随机 bearer，可按当前页面最小需要返回本地业务库已有的 `profileId`、候选人 displayName、职位、评分/筛选结果、生成招呼语、消息正文、简历摘要、沟通/邀面状态及候选人的权威 `ContactAsset` 微信号。微信号只允许来自已经收编的候选人联系资产，仍不得返回招聘方自己的微信号。前端不得把这些响应写入 localStorage、IndexedDB、普通日志、错误上报、支持包或导出文件；API handler 不得记录响应正文。此例外不授权 `/admin/*`、stdout、审计 detail、验收报告、远程接口或 AI 请求暴露上述明文，不授权返回 platformUserRef、accountRef、resumeNumber、原始完整简历、完整 prompt、provider 原文或任何密钥，也不改变业务数据库、`ai-traces.db` 与 AI provider 的既有边界。
- **开发者 SQL 控制台例外（2026-07-30 甲方裁决）。** 诊断台新增一个裸 SQL 页面：输入任意 SQL、由脑用自己的数据库连接执行、把结果或错误回显在同一页面。它**不设任何护栏**——这是甲方在知悉下述后果后的明示选择，执行方不得自行加回：不限制语句种类、不限制可读写的表与列、不做预览确认、不自动备份、不检查是否有活跃工作流。因此：(一) 它可以读出候选人 displayName、权威 `ContactAsset` 微信号、聊天正文、简历快照等本地业务库里的一切明文，本条就此把「同机产品 UI 业务投影例外」的同类授权扩到这一个 `/admin/*` 页面；(二) 它可以物理 `DELETE` 业务事实行、可以改写 WAL/证词/幂等/状态机字段。「业务事实行禁止物理 DELETE」与安全内核的其余条款在这个入口上**不再由代码保证**，仅靠使用者自律；这些条款对正常代码路径的约束一律不变，不得援引本条在别处放宽。(三) 脑运行期改库还可能与巡检的内存态不一致，风险由使用者承担。边界仍然收在同机：该页面只经既有 `/admin/*` loopback 监听与 admin bearer 提供，SQL 文本与结果只回显到同机诊断台，不得进入普通日志、审计 detail、stdout、支持包、验收报告、运营通知 webhook、旧后台、AI 请求或任何远程渠道；handler 不记录结果正文。本条只授权这一个页面，不改变其他 `/admin/*` 端点、`ai-traces.db`、AI provider 与云端出站的既有边界。它是开发期工具，首个真实客户交付前必须重新裁决是否保留、是否需要在客户环境下移除或加锁。
- **开发者诊断台明文边界（2026-07-31 甲方裁决）。** 把上一条「开发者 SQL 控制台例外」的明文授权从那一个页面推广到整个 `/admin/*` 诊断面：它可以返回本地业务库已有的一切业务明文，包括候选人 displayName、权威 `ContactAsset` 微信号、聊天正文、简历快照、原语 args/guards/result 原文与命令审计快照，供开发期人工诊断与 suspect 裁决使用。「同机产品 UI 业务投影例外」末句"此例外不授权 `/admin/*`……暴露上述明文"限定的是那次授权自身的适用范围，本条另行授权，不再有歧义。立案起因：suspect 队列此前只显示原语英文名，甲方无法判断一条待裁决对应哪个候选人、哪件事，裁决只能靠猜；而判错的贵方向——判 `resolvedFailed` 实则已发——会导致后续补发多发一条给真人，正是"宁可少发"红线最不该失守的地方。代价是诊断台成为本机业务明文的全出口，甲方明示接受，执行方不得在代码里自行加回逐字段白名单之类的护栏。边界仍然收在同机：`/admin/*` 只经 loopback 监听与 admin bearer 提供，响应只回显到同机诊断台；handler 不得记录响应正文，前端不得把响应写入 localStorage、IndexedDB、普通日志、错误上报、支持包或导出文件。本条**只放宽 `/admin/*` 这一个面**——stdout、普通日志、审计 detail、验收报告、`/app/*` 产品 API、运营通知 webhook、旧后台、AI 请求与任何远程渠道一律不因本条放宽，`ai-traces.db` 与 AI provider 的既有边界不变。API key、`Authorization` 及其他鉴权材料不属于业务明文，仍禁止经 `/admin/*` 回显。本条是开发期工具授权，首个真实客户交付前必须与「开发者 SQL 控制台例外」一并重新裁决是否保留、是否需要在客户环境下移除或加锁。
- **AI 动作建议是封闭枚举（2026-07-23 M5-B 集中立案）。** 回复模型的 `动作` 只可建议 `无`、`发起线上会议`、`发起换微信邀请`；它不是自由命令，也不能直接创建 effect intent。脑必须用已冻结的业务状态、动作余额与推荐时段重新裁决；邀面时间只能精确命中本轮冻结的规范化候选时段。未知动作、歧义/越界时间或不满足确定性前置时不得猜测执行，具体保守语义以 `docs/沟通逻辑规格-v4.md` 为准。
- **业务事实行禁止物理 `DELETE`(2026-07-20 甲方裁决)。** 已持久化的领域事实与审计事实不得通过物理删除抹去;业务上的“删除/移除”必须表达为带原因的显式状态或标记列,查询方按业务语义显式决定是否包含这些行;确需物理删除的例外必须按本文第 12 条单独立案获批。本条不要求全表增加 GORM `DeletedAt`,且禁止通过显式字段或嵌入 `gorm.Model` 引入默认 `deleted_at IS NULL` 作用域来替代业务语义;人级建档闸、对账与审计等需要看见终态/已移除事实的查询不得因此失明。临时测试数据、可完全重建的缓存/投影,以及协议已明确生命周期与清理语义的基础设施记录不属于业务事实行;不得通过改名或换表逃避分类。
- 常用命令(仓库根目录执行):
  - `go run ./contract/codegen` — 从 contract.v1.json 重新生成两端协议代码到 `contract/gen/`;**改契约后必跑**,产物一并提交。
  - `go run ./contract/codegen -check` — 校验产物与契约一致(CI 门禁,漂移退出码 1)。
  - `go run ./client/service` — 起脑服务(`-port` 默认 17872,`-data` 默认 `data/`,已 gitignore)。
  - `go test ./...` / `go test -race ./client/service/...` — 全部 Go 测试 / 竞态。
  - **JS 包管理用 pnpm workspace**(根 `pnpm-workspace.yaml` 统管 plugin / client/ui / client/electron)。仓库根 `pnpm install` 一次装全部;`pnpm approve-builds` 已固化到根 package.json 的 `onlyBuiltDependencies`(esbuild/electron 原生二进制)。禁止再引入 npm/yarn 锁文件。
  - `cd plugin && pnpm build` — 打包手端插件到 `plugin/dist`(unpacked 加载目录);`pnpm typecheck` strict;`pnpm test:node` 连真脑端到端(需先起脑)。
  - `cd client/ui && pnpm dev` — 起 UI(Vite,5273);`pnpm build` 构建;`pnpm test:node` 数据层连真脑。
  - `cd client/electron && pnpm start` — Electron 壳(启停脑 + 开窗);`pnpm test:node` 服务生命周期。
  - 人眼验收操作:见 `scripts/dev-run.md`;§16 验收对照见 `docs/里程碑1-验收报告.md`。
- Go 侧 SQLite 走 glebarez/sqlite(纯 Go);**禁止引入 cgo 依赖**,`CGO_ENABLED=0 GOOS=windows go build ./client/service` 必须常年通过(Windows 交叉编译是发布路径)。
- SQLite 写靠 `SetMaxOpenConns(1)` 串行化(SQLite 单写;红队复现过并发 BUSY 静默丢结果→双发的致命链)。
- 协议实现:唯一规范源 `contract/协议规格-v1.md`;里程碑 1 验收记录见 `docs/里程碑1-验收报告.md`。
