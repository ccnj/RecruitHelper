# 里程碑 5 · 批次 6 type 148 拒绝消息事实记录

> 采样日期：2026-07-22（Asia/Shanghai）
>
> 探针边界：只读当前已登录智联 IM 页；不发送、不点击、不导航、不改页面状态。探针没有返回或持久化候选人身份、会话号、职位号或服务端消息 ID 原文。
>
> 结论：**事实门通过，只授权实现 `custom type 148` 的严格候选人入站文本映射；不是 148 的类型不在本批扩大。**

## 一、探针结论

落仓只保留类型码和布尔结论：

| 事实 | 值 |
|---|---:|
| `customType` | `148` |
| `rawTypeIsCustom` | `true` |
| `uniqueTemplateMatch` | `true` |
| `candidateDirection` | `true` |
| `statusSuccess` | `true` |
| `structuredTemplateMatch` | `true` |
| `serverMessageIdPresent` | `true` |
| `pageGreetingBeforeRejection` | `true` |
| `ledgerSeqOrderCorrect` | `true` |
| `greetingTsApproxCorrect` | `false` |

生产映射只允许在上述形状同时成立时把该行表达为 `in/text`。平台稳定消息身份的原文永不出手；手只按协议规格 §4.5 的冻结配方生成不可逆 `sourceKey`，供脑在同会话域内对账去重。缺稳定身份、方向不明、状态非成功或结构不完整时继续走保守分支，不猜测同一类型的其他形态。

## 二、M5-B 短路候选样本

登记首个当前真机拒绝模板样本 `M5B-RT-001`：

> 很抱歉，我暂时不考虑这个机会，感谢您的认可~

该样本语义为候选人拒绝，只取得 **M5-B 立项时的候选规则样本** 资格。M5-A 的 `enabledRuleIds=[]` 保持不变，本批不得据此启用代码短路、绕过意向 provider 或自动发送拒绝后话术。

## 三、已知时间错误

既有招呼行不回写。批次事实明确记录：**此行时间为已知错误，成因是确认时刻冒充消息时间。** 页面事实顺序与消息账本 `seq` 顺序均为招呼在前、type 148 拒绝在后；不得根据错误的 `tsApprox` 忽略、重排或吞掉拒绝消息。

实现级修正只阻止今后继续把 effect 结果的 `observedAt` 投影成消息 `tsApprox`；`observedAt` 仍表示取得副作用正证的观察时刻。`tsApprox` 既有契约为 optional/nullable，本批不修改协议或 machine contract。
