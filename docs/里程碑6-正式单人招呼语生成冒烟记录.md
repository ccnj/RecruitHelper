# 里程碑 6：正式单人招呼语生成冒烟记录

日期：2026-07-22

## 范围

本轮只验证已完成配置化筛选的正式批次：只为该批唯一 `selected` 成员读取冻结的旧后台“招呼语”提示词，调用本地配置的 AI provider 一次，并把合法正文作为本地业务事实持久化。

批次：`sb-36ce0a47b687d65640fe6374`；配置修订：`cfbba4f16c895ea065cd1d3047a6ecd2941d93cddf14c698a35cbb720bb9752c`。本轮不调用手端、不打开平台页面、不创建 greeting head、EffectIntent 或 effectful command，也不发送消息。本文不记录候选人身份、简历、prompt 或生成正文。

## 真实执行结果

| 检查项 | 结果 |
|---|---|
| 批次入选 / 本次调用终局 | 1 / 1 |
| 成功 / 失败 / 待处理 / 进行中 | 1 / 0 / 0 / 0 |
| provider / model | `deepseek` / `deepseek-v4-pro` |
| 输入 / 缓存输入 / 输出 token | 3351 / 0 / 263 |
| 估算费用微单位 | 1686 |
| 正文 UTF-8 bytes / 上限 | 957 / 2048 |
| 输入 hash / 正文 hash / 原始输出 hash | 均为 64 位 SHA-256 |
| reasoning 用量形态 | `reasoningFieldAbsent`；非空 `reasoning_content` 阻断闸未命中 |
| 候选人正文、姓名、逐人引用进入管理响应或本文 | 无 |

正式调用返回一条满足当前输出契约的 `招呼语`，经 NFC、非空、UTF-8、长度与非思考闸后终局为 `ok`。分析字段没有进入业务账本。

## 幂等与零发送核对

1. 执行前，本批 `sourcing_greeting_invocations` 为 0；首次执行后为 1 条 `ok`，正文、输入和原始输出摘要均完整。
2. 对同一 `batchId` 立即重复 POST，返回的成功/失败计数、token、费用和完成状态完全不变；调用事实仍为 1 行，没有二次 provider 调用或正文增生。
3. 执行前后，全库 `ai_invocations=0`、`sourcing_score_invocations=13`、`candidate_greeting_heads=6`、`effect_intents=6`、effectful `cmd_records=6`、`messages=9`，全部未增加。
4. 管理端响应只包含批次、配置修订、聚合状态、provider/model、token 和费用，不返回 profile/run/invocation 明细、简历、prompt 或正文。
5. 独立阻断级审查未发现跨批/错 revision、非入选成员调用、重复 provider 调用、非法正文入账、隐私泄漏或误入手端/发送路径的问题。

## 门禁与结论

- `go test ./...`：通过。
- `go run ./contract/codegen -check`：通过；本阶段没有修改脑手契约。
- 正式单人招呼语生成及同请求幂等复跑：通过。

“正式批次筛选名单 → 后台招呼语 prompt → AI 个性化正文 → 唯一终局账本”的竖切已经变绿。下一阶段可以使用稳定平台用户 ID 在当前推荐列表重新定位该成员，直接点击列表卡片的“打招呼”按钮；只有那一阶段才允许创建发送意图并进入既有 WAL/idemKey/正证轨道。
