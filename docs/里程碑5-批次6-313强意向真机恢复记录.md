# 里程碑 5 · 批次 6 · 313 强意向真机恢复记录

日期：2026-07-23（Asia/Shanghai）

## 一、范围

本次只恢复一条已经确认由旧 UTF-8 字节预算闸误判的 M5 回复轮次。恢复前账号处于 `userStopped`，旧 `selection`、旧 `reply attempt=1` 与原消息账本均保持不变；恢复不授权第三次 provider 调用，也不绕过既有 WAL、幂等、证词和正证轨道。

记录只含随机业务引用、类型、状态、计量和布尔结论，不含候选人身份、聊天正文、简历正文、完整 prompt 或 provider 原始回包。

## 二、硬切换

- 脑以当前代码和原数据目录重启。
- 插件经 `debug.reload@1` 维护握手自重载，新 `bootId` 已出现。
- 脑手 `contractHash` 一致，插件版本为 `0.1.0`。
- 当前平台身份经只读探测重新绑定到新 `bootId`。
- 恢复授权写入期间账号保持停止；授权本身没有调用 provider、没有派发命令。

## 三、真实结果

| 事实 | 结果 |
|---|---:|
| 旧 `reply attempt=1` | `budgetBlocked/budgetBlocked`，所有 usage、延迟和费用均为 0 |
| 恢复调用次数 | 1 |
| 新 `reply attempt=2` | `invalidOutput/invalidOutput` |
| provider 输入 token | 7315 |
| P 档输入上限 | 16000 |
| 输入 token 余量 | 8685（约 54.3%） |
| provider 输出 token | 199 |
| provider 延迟 | 5014 ms |
| reasoning usage 形态 | `reasoningFieldAbsent`，未命中 reasoning 不安全闸 |
| 输出摘要 | 64 字符 SHA-256，非空 |
| 自动沟通 action | 0 |
| effect intent | 0 |
| `chat.sendMessage` 命令 | 0 |
| 新增 self message | 0 |
| 活动消息账本 | 原招呼 1 条 + 候选人 313 简历卡 1 条 |
| 最终状态 | turn 与新 selection 均为 `manualRequired/replyFailed` |
| 账号状态 | `userStopped` |

`projections=0` 只表示本轮线程读取没有产生新的消息账本投影；同一巡检随后确实拾取了已恢复的 `classified` turn，创建并完成了唯一 `attempt=2`。失败点是回复建议没有通过 `ParseReplySuggestion` 的既有严格输出契约，不是恢复触发、token 预算、页面读取或发送链失败。

出于 provider 数据边界，系统没有持久化或记录原始回包，因此本次不能据此判断是 JSON 外包裹、未知字段还是字段类型不符。没有事实证据时不放宽解析器，也不追加新的自动重试。

## 四、结论

1. P 档以 provider `usage.prompt_tokens` 裁决已经在真实 V4 Pro 响应上生效；原 27.5 KiB 中文请求没有再被字节数误拦。
2. 一次性恢复机制正确保留旧事实、只授权 `attempt=2`，provider 失败后永久收敛为人工，没有生成 `attempt=3`。
3. 本次没有走到发送，因此“313 强意向 → AI 回复 → WAL 发送 → 服务端正证”的完整真机出口仍未通过；应等待下一条合法候选人轮次重新验收，而不是复用本轮继续调用。
4. `invalidOutput` 属可恢复、有人兜底的 provider 输出瑕疵；在出现可复现样本或失败率数据前列为观测项，不阻断已经通过的简历复用、弹窗关闭和 token 计量修复。
