# 智联真机事实（2026-07-17）

本文只记录在当前已登录真实智联招聘方 Chrome 中实际观察并复核过的事实，作为里程碑 2 智联 program 的实现依据。候选人姓名、招聘方姓名、企业名、手机号、微信号、职位编号、账号 ID、会话 ID、token 和消息正文均不进入仓库。

## 1. 证据边界

- 观察日期：2026-07-17（Asia/Shanghai）。
- 页面：当前用户 Chrome 中已经登录的真实招聘方账号。
- 主站 host：`rd6.zhaopin.com`。
- IM 路由：`/app/im`；选中会话后 URL 为 `/app/im?sessionId=<opaque>`。
- 账号页路由：`/app/personal`。
- 页面运行时公开加载的三个脚本也用于静态复核：
  - `app._route_.im.web.a480807d6f2a87d367d112aeb9c664cb.js`，SHA-256 `9ee07db0749391256a1218c48470f8afeb03ea8737c114dd5b76c0d1da799ed2`；
  - `im-base.web.911a7ee28fee13f27deb4a0bd812bf79.js`，SHA-256 `85500bbbb3755f0430e38c788c35c415447af9b376c7d1a3ce09150f9f0a218a`；
  - `app-store.web.6ac72d412b46c420f4735f0dee0407ee.js`，SHA-256 `b2eb4b64da37936589f261e843447da3130a469f4974d9dd6e6e5dc1c30a477c`。
- 上述 hash 只定位本次观察到的页面版本；智联发版后必须重新侦察，不能把文件名或 selector 当永久契约。

## 2. 页面、登录与账号身份

### 2.1 可观测页面状态

- 正常登录页面存在 `#root`，并有主导航 `/app/job`、`/app/recommend`、`/app/search`、`/app/im`、`/app/personal`。
- 页面 HTML 内有一个以 `__INITIAL_STATE__=` 开头的 JSON 启动脚本。
- 登录信号是 `session.session.isLoggedIn === true`，同时 `session.session.staff.staffId` 与 `session.session.org.orgId` 可用。
- 浏览器隔离执行环境中 `window.imEngine`、`window.$session`、`window.__INITIAL_STATE__` 和 `window.Vue` 都不可见；但启动脚本的 DOM 文本可见。因此动态调用页面 IM 引擎需要显式 MAIN-world bridge，不能假定普通 content script 能直接访问页面全局变量。

### 2.2 opaque principal fingerprint

最终扩展重载后，当前 IM 页确认以下三个字段同时存在：

- `session.session.staff.staffId`
- `session.session.org.orgId`
- `session.session.staff.defaultLoginPoint`

同页的 `personal.imUserInfo` 实际为空对象，因此早期依赖 `rootCompanyId` 的实现会得到 `loginState=in` 但 fingerprint 为 null；正式绑定真机因此被正确拒绝。现行 program 以 session `orgId` 为主锚，只在旧页面缺少它时才用 `personal.imUserInfo.rootCompanyId` 作为保守兼容兜底。字段在 MAIN world 用固定版本前缀和无歧义长度编码形成输入，再做 SHA-256，只把摘要返回给脑：

```text
sha256(join("|", [byteLen(value) + ":" + value for value in
                   ["zhilian-principal-v2", staffId, orgId, defaultLoginPoint]]))
```

脑只比较完整摘要，不解析内容。任一字段缺失时身份状态为 `unobservable`，不能退化为姓名、公司名、手机号、Cookie 或 token。真机已证明该摘要能完成零配对正式绑定；尚未用第二个账号实测“切换账号必变”，在有第二账号前将该项保留为未销风险，并以三字段中任一变化即 mismatch 作为止损。

## 3. 会话列表

### 3.1 启动状态字段

`__INITIAL_STATE__.im.sessions` 是当前首批会话。每条已确认包含：

- `sessionId`：32 字符 opaque 会话标识；
- `peerPartnerId`：稳定候选人平台引用，作为 `platformUserRef`；
- `userId`、`typeUserId`：页面内部其他候选人引用；
- `jobNumber`、`jobTitle`；
- `unreadCount`、`isUnRead`、`unreadReminderType`；
- `lastSentence`、`lastMessageReadTime`；
- `sortTime`、`createdDate`、`modifiedTime`；
- `resumeNumber`，但它只可作证据，禁止作为候选人主键。

首屏实测 20 条，`pageNo === 2`、`hasMoreSession === true`，`sortTime` 降序。页面代码调用 `im/session/list`，参数含 `pageNo`、`pageSize: 20`、`activeSessionId`、筛选条件等。

### 3.2 DOM 与虚拟滚动

已验证的当前页面结构：

```text
.im-session-list
  .im-session-list__virtual
    .im-session-list__virtual--box
      .im-session-item
        .im-session-item__name-title
        .im-session-item__msg
        .im-session-item__time
        .im-session-item__unread
```

- 首屏虚拟容器高约 907px、内容高 1440px，对应 20 条、每条约 72px。
- 第一次滚动加载后内容高从 1440px 增至约 2922px；继续滚动后依次增至约 5769px、7218px。
- DOM 只保留约 32 个挂载项，不能用“当前 DOM 元素数”判断总条数或 `complete`。
- 首批消息很多，仅滚动到约 100 条时可见日期仍在前两天。因此 8 天窗口可能需要多页，`chat.readList` 必须逐页/逐滚动上报 progress，并由 cutoff、`maxSessions` 和真实 `hasMore` 共同确定 `complete`。

### 3.3 未读语义

- `.im-session-item__unread` 的文本和 `sessions[].unreadCount` 一致，可为字符串呈现的 `0`。
- 左侧主导航总未读徽章已验证为 `div.app-im-unread.app-menu-item__im-unread`，父节点为 `.app-menu-item-content-normal__tip--count`；当前版本是纯数字文本。元素缺失能否一律解释为 `0` 仍须双读与导航状态共同判定，不能单次缺失即发零值。
- 顶部全局未读徽标不是强一致信号：打开会话后行内未读已从 `1` 变为 `0`，顶部徽标仍短暂保持旧值；刷新后才反映新状态。
- 因此列表结果以每条会话的 `unreadCount/isUnRead` 为准；顶部徽标只作 QoS0 提示，不作账本。

## 4. 线程读取与已读副作用

### 4.1 页面真实实现

智联页面的时间线主路径是：

```text
window.imEngine.getHistoryMsgs({
  to: activeSession.peerPartnerId,
  lastMsgId: oldest.idServer,
  endTime: oldest.time,
  scene: "p2p" | "team"
})
```

失败时页面回退到：

```text
POST im/getSessionMsgs {
  targetType: activeSession.sessionType,
  targetId: activeSession.userId,
  endTime
}
```

页面用 `idServer` 去重并按 `time` 正序合并；回退路径还用 `sendMessageId` 做第二次去重。历史区页面明确显示只提供近 90 天消息。

最终真机重验还确认了当前生产页的实际兜底形状：

- 时间线容器是 `.im-timeline__wrapper .im-timeline`，不是旧版 `.km-list`；
- Vue2 `__vue__` 数据在当前生产构建中不可用；
- 同一路由启动状态的 `im.timelineMap[sessionId].timeline` 含稳定消息身份，可作为 MAIN 引擎不可用时的只读数据证词；
- 明确边界节点是 `.im-timeline-ending`，当前文案为“以下是90天内的聊天消息”；
- MAIN runtime 的 `$session` 可能暂时为 `null`，此时招聘方 `staffId` 必须回退到同一份已解析的 `__INITIAL_STATE__`，不能猜方向。

program 只有在该显式 90 天边界存在且当前 eligible 行数不超过本页上限时才声明 `reachedTop`；超过上限时继续生成排他 cursor，没有边界或数据身份不稳定时响亮失败，不把可见窗口伪装成完整历史。

### 4.2 首次打开会话会产生外部可见语义

页面在首次时间线读取后会调用/触发：

- `window.imEngine.sendMsgReceipt(...)`；
- `im/send/lastMsgRead`；
- 条件满足时 `im/send/refreshMsgRead`；
- `im/session/markRead` 与 IM SDK session ack。

真机实测：打开一个未读数为 `1` 的既有会话后，该行未读立即变为 `0`，刷新后仍为 `0`。所以 `chat.readThread` 已在正式协议中定为 intrusive：只允许 tracked 会话、受账号串行域和用户静默窗约束，平台副作用声明为 `idempotentReadReceipt`。不得为了索引未知会话而逐个打开。

如果 MAIN-world bridge 后续能证明“只调用 `getHistoryMsgs` 且不调用 receipt/markRead”不会改变平台状态，可以增加无已读路径；在真机证明前不得假设无副作用。

## 5. 消息形状

### 5.1 时间线基础字段

`im.timelineMap[sessionId].timeline[]` 已确认包含：

- `from`、`to`、`scene`、`sessionId`；
- `idServer`（样本为 14 字符）与 `sendMessageId`（样本为 32 字符）；
- `time`、`status`、`type`；
- `text`、`file`、`content`。

方向以 `from === current staffId` 判定为招聘方发送，否则为候选人发送。M2 入账的 `origin` 仍是产品语义：历史和真人手发均记 `external`；只有未来系统 intent 成功产生的消息才记 `self`。

### 5.2 DOM

线程根与消息节点：

```text
#im-session-detail.im-session-detail
  .im-timeline__wrapper
    .im-timeline
      .im-message
```

文本消息使用 `.im-message__bubble`、`.im-message__text`，招聘方气泡带 `.im-message__bubble--me`，已读状态带 `.im-message__read-state`。系统提示使用 `.im-message__toast`。DOM 只作 MAIN-world/API 失败时的兜底与证据，不应成为消息身份主来源。

### 5.3 已见 custom 类型

当前历史样本已验证以下 custom `content.type`：

| type | 已观察语义 | 关键子字段 |
|---|---|---|
| `104` | 当前沟通上下文 | `job`、`resume`、`sessionOriginType` |
| `169` | 沟通职位系统提示 | `jobNumber`、`jobTitle`、`rootOrgId`、`title` |
| `131` | 招呼消息 | `greetingText` |
| `105` | 换微信请求卡片 | `scene: EXCHANGE_WECHAT`、`staffContent`、`userContent` |
| `99` + `content.type: 2` | 对方拒绝换微信 | `msgb`、`msgc`、`type` |

普通文本的 NIM `type` 为 `text`，正文在 `text`。未知 custom 类型必须保留稳定 hash，并按正式枚举归为 `system`（文本使用不含隐私的稳定类型占位或页面可见摘要），不能静默丢弃或误判为普通文本。

页面当前换微信卡片组件还会以 `{sessionId, messageId}` 调用只读接口 `im/getWxCard/state`，返回数字状态用于渲染 `EXCHANGING/DONE/REFUSED`。这说明卡片当前状态不能只从尾部拒绝提示推导；正式 program 应以原始请求卡片的稳定 identity hash 为身份、接口状态为可选状态证据。该接口的直接 `fetch` 仍待本插件 MAIN-world 真机验证，失败时只能返回 `unknown`，不能伪造终态。

### 5.4 M3 发送执行期的实时数据源与动作信任边界

本节承接 2026-07-20 从平台中立协议规格移出的智联专有判据。以下 `timelineMap`、`idServer`、`peerPartnerId`、Vue root 与 `__INITIAL_STATE__` 结论只约束智联 program,不得外推成其他平台的协议语义。

2026-07-20 的真实页面只读诊断补充确认：`getHistoryMsgs` 适合 `chat.readThread` 的执行外对账，但在发送 preflight 中连续调用会稳定出现拒绝/不可用，不能把冷却等待或重试当作发送安全证明。正式 `chat.sendMessage` 的发送前基线、点击同步栈和点击后观察因此统一为 **0 次 history API 调用**，只读当前唯一存活 Vue root 的确切 `root.$store.state.im.timelineMap[conversationRef].timeline` 槽；`__INITIAL_STATE__` 和 DOM 均不得充当发送执行期消息身份源。

2026-07-20 的《防护成本预算》第 9 条复审保留这条实时感知通道，但删除平台私有层互证。重整后的规范路径如下；实现、全量自动门禁、甲方/Claude 独立 diff 和新的有人值守真机验收均已完成：

- baseline 在任何 `await` 前单次逐行深值投影；删除固定 250ms 第二样本和会话版本 token。完整源仍要求每行有非空 `idServer`，且 `Number(time)` 是有限正数；
- baseline 按 `time` 正序、相同 `idServer` 保留首行，再取 last64；有序 server-ID 窗和 `expectedTail` 必须精确相等；
- 有序 source keys、`expectedTail` 和 `hash(conversationRef, peerPartnerId)` 的 `targetBindingToken` 被传入唯一同步 action。生产 preflight 与 commit 字面调用同一 MAIN 函数、同一组参数和同一份 evaluator；在 attempting 前、commit 输入前、input/change 后分别复核账号、route、硬截止、唯一可见控件、composer 值、source keys、expectedTail 和目标 token。target token 只保证各次读取仍是同一世界目标，不与 Sender owner/model/engine/button 接线互证；
- final evaluator 绿色后不再读取页面，立即调用一次标准 click，否则清理自动草稿且零点击；点击后只接受有序 last64 相对 baseline 严格连续新增一条。基线少于 64 条时是原序列后追加一条，满 64 条时是左移一条后追加一条；唯一新行还必须是 `status=success`、我方方向、`type=text`、正文 hash 相等，并在所有 observer 轮次保持同一 `targetBindingToken`；
- `chat.readThread` 只保留给执行外的歧义恢复；阴性、多个匹配、窗口不连续或读取失败都不能授权自动重发。

发送控件的合法硬闸止于公开边界：唯一可见 detail/editor/button 及 DOM 包含关系；按钮关联 form 时必须显式 `type="button"`，避免标准 click 同时产生 default submit。原生 `disabled` 只会让标准 click 无效，`aria-disabled` 没有标准阻断语义，二者都不满足增量三问，不再作为硬闸。Vue owner/component tree/model/engine/VNode/listener 及其互相一致性不再读取、观测或授权。实现仍只调用一次标准 `HTMLElement.prototype.click`，但不拿它证明平台送达；平台动作结果只由即时后置核验与配对验证读收敛。它们可以发现并升级零新增、多新增、目标换绑或读不清，不能撤销已经发生的伤害，也不授权第二次 click。真机此前还确认左侧 `.is-active` 出现在在线状态子节点，它不是“当前选中会话”的证据。

## 6. 当前实现裁决

- `probe.platform`：DOM 判断 host/route/content script；MAIN-world bridge 读取登录状态与身份三元组，只返回 SHA-256 fingerprint。
- `nav.ensureSurface`：复用 `rd6.zhaopin.com/app/im`；无页面时才创建；绝不自动登录。
- `chat.readList`：优先 MAIN-world 调用页面已有 `imEngine.getSessions`；平台请求可使用更小内部页，协议对外页最多 32 条且 data≤64KB。每条 conversationRef 以 opaque `sessionId` 表示，platformUserRef 以 `peerPartnerId` 表示。已实现保守 DOM 虚拟列表兜底：首窗只接受启动状态，动态窗只接受带稳定身份的 Vue source；只有按降序越过 8 天 cutoff 才宣告 complete，到可见底部但没有平台 hasMore 证词时响亮失败。正式 actor 已在最终插件上连续分页并建立 509 条轻量索引。
- `chat.readThread`：优先 MAIN-world `getHistoryMsgs`；按 `idServer`、`sendMessageId` 做手内去重，脑只看 direction/contentHash 做最大重叠。协议对外页最多 64 条且 data≤64KB；引擎不可用时只接受目标路由上具有稳定消息身份的 Vue 数据或同一启动状态 `timelineMap`，并要求显式 90 天边界才声明 `reachedTop`。最终正式 actor 首次读取收编 7 条历史并把收编边界设为 7；第二轮以 5 条锚尾完成重叠证明，消息总数仍为 7、本轮新增仍为 0。2026-07-22 的真实 M5 巡检又证实：在 `/app/im` 基础路由上，目标会话虽可由 `getSessions` 唯一解析，`getHistoryMsgs` 仍可能稳定拒绝，DOM 兜底也因目标 route 未选中而无权读取；因此“引擎读取与当前 route 无关”不是可依赖事实。正式 program 必须在目标尚未选中时先按完整 `conversationRef` 唯一定位并以一次标准会话行点击打开目标，再读取历史；已有草稿、账号/route 变化、非唯一目标或点击后 route 未确证均响亮停止。打开会话可能产生已读，仍由既有 `intrusive + idempotentReadReceipt` 契约覆盖，不新增原语或协议字段。
- `debug.inspectSendSurface`：经正式脑→手 readonly 通道只诊断粗粒度世界状态与消息感知通道并返回 `ready/stage`；不读写输入框、不点击、不返回路由、身份、候选人、正文、hash 或原始异常，也不遍历平台私有组件接线。当前实现唯一可能返回 `page_absent`、`route_missing`、`composer_cardinality`、`detail_cardinality`、`button_cardinality`、`dom_containment`、`button_form_unsafe`、`draft_present`、`thread_unavailable`、`diagnostic_unavailable`、`ready`；旧私有互证与旧 baseline 阶段码已在首客前契约清理中删除。它不是生产 preflight，不参与真实发送授权。
- `chat.sendMessage`：只接受人已打开的 tracked 会话。执行期消息事实仅来自 §5.4 的实时 timeline；DOM 只唯一定位可见控件。账号、route/目标 token、source keys、expectedTail、composer 和 form/type 任一不满足都在唯一 click 前闭锁；平台私有层不互证。点击后歧义才进入即时后置观察、`chat.readThread` 与 suspect，永不补 click。
- 页面脚本 hash、selector、custom type 都属于智联 program 实现事实，不进入平台无关协议语义。

最终真机还验证：扩展重载后稳定 `handId` 重新连入、页面暂不可用时 actor 经一次 `nav.ensureSurface` 恢复；真实用户点击事件在长 `chat.readList` 途中建立 45 秒静默窗，并在列表结束后阻止新的 `chat.readThread`，轮次以 `USER_ACTIVE` 响亮终止，既有 7 条账本不变。同一持久化数据库重启脑后，插件在两个同时打开的 IM 标签下只建立一条 hand session，actor 按持久化的“今日开启”状态自动恢复完整巡检，`chat.readThread` 再次终局 `ok`，消息仍为 7 条且新增投影仍为 0。

唯一增量验收时，脑先优雅退出，招聘方再由真实页面人工发送一条自然文本，页面确认只出现一条；随后关闭发送所在标签，只保留另一个 IM 标签。因为手端事件没有持久 outbox，脑离线期间的发送/导航提示必然全部丢失。重启脑时账号 `dirtyHint=false` 且原定 5 分钟巡检已经到期，因此轮次以 `timer+surfaceRecovery` 触发，经一次 `nav.ensureSurface`、完整 `chat.readList` 和 `chat.readThread` 后把账本从 7 条推进到 8 条，本轮新增恰为 1。距上一轮满 60 秒后从同一 actor 入口复读，列表尾已与账本一致，没有再次打开会话，账本仍为 8 条且本轮新增为 0。该链同时证明事件全丢、发送页关闭、同日脑重启和重复对账不增生；不证明 M2 已具备产品自动发送原语。

2026-07-20 的旧 M3 路径最终真机验收又补充了生产自动发送事实。第五次真实命令在平台 click 前以 `ELEMENT_UNRESOLVED / sideEffect=none` 停止，根因是粗粒度诊断与最终动作栈分叉，后者额外用 DOM 文本 selector 判断包含 text/system/card 的混合尾部。删除该冗余 DOM 消息语义硬闸并合并为共享 evaluator 后，第六次命令经正式产品入口只提交一次：实时 Vuex timeline 观察到严格 +1 的唯一新服务端成功文本，命令终局 `ok`，脑侧 `origin=self` 恰好一条，suspect/outbox/attempting journal 均为零。随后脑优雅重启、手自动重连并重新核验，发送命令、成功 intent 和 self 消息数量均未增生。该段固定为旧路径事实，不与后续 guard cohort 合并。

第 9 条信任边界重整经甲方/Claude 独立 diff 审查通过后，第七次真实命令再次由正式 RecruitHelper 产品入口只提交一次。发送前身份 current、目标为唯一 adopted 会话、产品编辑器只含获准的两字自然问候，页面没有验证码、风控、真人草稿或静默阻断；浏览器控制没有直接操作智联发送按钮。共享 evaluator 的 preflight/commit 均绿色，最终标准 click 恰好一次，实时 timeline 与真实页面均观察到严格 +1，命令终局 `ok`。完整账本由发送命令六条（失败五、成功一）推进为七条（失败五、成功二），effect intent 和 `origin=self` 也都只增加一；suspect/outbox/open journal 均为零。随后脑优雅重启、手按相同契约自动重连，发送命令、effect intent、`origin=self` 和页面可见消息均无增量，witness 仍 ready 且 outbox/open journal 为零。该事实证明当前重整在有人值守、既有 adopted 会话和普通文本范围内通过；不外推到新会话招呼、无人值守、第二账号或第二平台。

## 7. 尚未销案的真机项

- 无第二个真实账号，尚未实测账号切换后的 fingerprint 变化。
- 尚未找到“同意换微信/接受卡片”的可控历史样本；拒绝样本已验证。接受状态在有历史样本或候选人配合后再销案。
- 图片、语音、文件消息的真实历史样本尚未逐类验证；未知类型必须保真保存并报告。
- 50/500 条完整线程快照的体积与时长尚未测得；协议在测量前必须使用分页/分块，禁止静默截断。
- 当前真实样本已覆盖一条普通文本增量；卡片接受、媒体、第二账号和长线程等未销项以验收报告的残余风险矩阵为准。
- 当前重整已完成独立 diff、一次真实单次自动发送和脑重启/手重连不增生验收；新 guard cohort 仍只有 `n=1`。现有证据只覆盖有人值守、既有 adopted 会话和普通文本，不覆盖新会话招呼、无人值守、第二账号或第二平台。

## 8. M4 主动建联真机事实（2026-07-20 增量）

完整脱敏探针过程见 `docs/里程碑4-批次0-真机事实记录.md`；本节只固化智联 program 可依赖的平台事实，不记录候选人姓名、原始 userId、职位编号、会话号、正文或截图。

- 推荐列表本身没有唯一 current/selected 语义；真人点开后，当前唯一可见 `.new-shortcut-resume__modal` 才定义“当前候选人”。正式读取不得选首卡、滚动或自动打开目标。
- 公开 DOM、URL 与页面初始化 JSON 都不能产出未建联候选人的平台 userId。经一次获批的只读 MAIN-world 脱敏探针，当前 20 个 listitem 的 `__vue__._props.source.userMasterId` 全部非空；代表目标同页两次摘要一致，轻刷新后在 20 个摘要中唯一命中一次。该结论只覆盖推荐页 listitem，不推翻 §4.1 对 IM timeline Vue2 `__vue__` 不可用的页面范围结论。
- 详情弹层挂在 `body` 下，不属于任何 listitem。经补充只读探针确认，URL `resumeNumber` 与 20 个 listitem Vue source 的 `resumeNumber` 恰好唯一匹配，命中 source 的 `userMasterId` 也在同窗唯一。因此它只可作为同一次 MAIN-world 读取中的瞬时详情→来源卡连接键；不得返回、持久化、作为 Candidate 身份或失败降级值。
- 因此智联 `candidate.readCurrent` 可把唯一命中 source 的 `userMasterId` 规范化为生产 `platformUserRef`。该来源是高脆身份感知通道，不是动作授权互证；读取失败、空值、重复、绑定歧义或结构漂移时必须读失败→不确认→转人工，禁止降级为 `resumeNumber`、姓名、头像、列表索引或 DOM 位置。
- `positionRef` 由 URL jobNumber、当前来源卡 Vue owner 的 root route query 与同一 owner 的 Vuex `talent.activeJob` 三者相等共同确定；candidate search-condition active job 在本次页面是可达的陈旧值，不得参与授权。标题只展示，不作身份。生产实现使用固定字面路径，不保留探针用的通用对象图扫描器。
- 未建联的公开世界正证是唯一当前详情中唯一可用的“打招呼”标准按钮；若身份已确定但关系仍不能确定，只能返回 `contactState=unknown`，不得发送。
- 招呼是已证实的两步拓扑：详情内第一击只打开唯一 `.ai-greeting-modal`；选择非 AI 统一招呼并编辑 textarea 后，footer 唯一“发送”按钮才是候选人可见的不可逆动作。统一招呼已选中而指针未悬停时，唯一 edit icon 只在样式层隐藏、DOM 节点不变；program 可在 attempting 前对该唯一可逆入口调用一次标准 click，但仍须观察到唯一可见 textarea 才能继续。“设置为默认，不再弹出”是独立账号级配置，M4 永不修改。
- 建联后的唯一会话按 candidate userId + jobNumber 绑定。服务端招呼 raw `type` 为字符串 `custom`，数字 `131` 位于 content envelope 的 `type`，正文位于 `greetingText`；正证还必须有稳定 `idServer`、`status=success`、我方 `from` 与正文 hash。timelineMap 容器键/sessionId 可作 conversationRef；消息行自身 sessionId 在现有样本中不等于 conversationRef，不得误用。system/job 行不计候选人可见招呼。
- 批次 6 的首次真实建联又暴露了新会话的服务端视图短暂分叉：候选人可见动作只发生一次，页面已出现唯一新会话和自然问候，关系按钮与当日配额也同步变化；但不刷新 IM 的 SPA 上下文时，正式 `chat.readGreetingOutcome` 连续只能得到阴性。确认两个可见 composer 均为空后轻刷新到同一新会话，页面初始化状态中的候选人、职位和会话引用全部唯一相等，目标 timeline 恰有一条 `idServer` 非空、`status=success`、我方 `from`、`custom/131` 且正文 hash 相等的招呼。阴性读数仍不表示未发送，也不授权第二次动作。
- 因此智联招呼正证不得把 `getHistoryMsgs` 当作排他的即时视图：同一 `conversationRef` 下，history API、当前 live timeline 与当前路由初始化 timeline 都只提供正证候选。program 只汇合满足完整服务端条件的行，并按 `idServer` 去重；所有可用视图的正证并集恰为一个稳定消息身份时才可确认，同一身份在多个视图重复不增计，零个或多个身份仍返回 `confirmed=false`。这只修正平台读取收敛，不改变协议字段、验证轮数、动作次数或“阴性不补发”的安全语义。
- 2026-07-22 按 AGENTS.md“可见后置状态原则”重裁决：前两次正式招呼均观察到同一目标由唯一“打招呼”变为唯一“继续沟通”，且之后 IM 中各只有一条实际招呼；该可见关系跃迁自此升级为 `chat.sendGreeting` 成功正证。上条 IM 服务端行仍是丰富正证与后续会话收编材料，但不再作为每次招呼成功的必经条件。关系无变化、unknown、目标丢失或读取失败仍只是不确认，绝不反推未发送或授权重发。

批次 0 从未点击最终发送。以上事实只授权 M4 按规范实现候选人读取、一次最终招呼动作和配套正证读取；正式首发仍须在批次 4/6 单独验收。
