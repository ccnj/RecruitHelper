# 里程碑 4 · 批次 0 真机事实记录

> 日期：2026-07-20。
> 状态：**未出闸**。动作拓扑、职位引用、自定义正文和既有会话正证已通过；“未建联候选人的稳定平台 userId 能否在发送前由新插件真机读取”尚无直接正证，批次 1 暂不启动。
> 边界：只使用当前已登录的智联真实账号做有人值守事实探针；未建档、未修改数据库、未修改契约或生产代码。
> 隐私：本文不记录候选人姓名、头像、简历、账号、职位名/编号、平台 userId、resumeNumber、conversationRef、消息正文、哈希或截图。

## 1. 出闸结论

| 事实门 | 结论 | 依据 |
|---|---|---|
| “当前候选人”的公开界面 | 通过 | 推荐列表同时有 20 个 listitem、当时视口内 4 个，没有 `selected/current/focus` 信号；点开简历后只有一个可见详情弹层，可作为真人当前目标 |
| 候选人关系未建立 | 通过 | 唯一当前详情内有且仅有一个可用的“打招呼”标准 `button[type=button]`；已建联的正证另由稳定会话关系给出 |
| 稳定 `positionRef` | 通过 | URL `jobNumber`、route query、talent active job 三者相等，可见职位标题与 talent active job 绑定，轻刷新后不变 |
| 第一击是否只打开编辑器 | 通过 | 当前候选人的“打招呼”只点击一次，页面打开唯一 `ai-greeting-modal`；没有触发最终“发送”动作 |
| 自定义正文 | 通过 | 弹层有两个招呼选项；无 AI icon 的统一招呼选项可展开唯一 textarea，获准两字短问候按 UTF-8 精确读回为 6 bytes |
| 不改默认配置、不留草稿 | 通过 | “设置为默认，不再弹出”全程未选中；恢复原文后关闭、复开，textarea 为原文且不含探针正文 |
| 既有会话的候选人/职位/会话绑定 | 通过 | 当前 IM 会话唯一匹配 URL session；同一 session 的 `userId/typeUserId/peerPartnerId` 归一后相等，简历详情 candidate userId 与 jobNumber 同时匹配 |
| 历史招呼的服务端正证 | 通过 | 既有 timeline 有且仅有一条招呼样本；稳定 `idServer`、`status=success`、我方 `from`、`greetingText` 均存在，会话/候选人/职位/招呼指纹轻刷新后全部不变 |
| 未建联候选人的稳定 `platformUserRef` | **未通过** | 可见详情、候选人卡、URL 与初始化 JSON 均不公开 candidate userId；旧项目的 `listitem.__vue__._props.source.userMasterId` 只是候选线索，当前只读浏览器观察层与页面 MAIN world 隔离，无法对其值和轻刷新稳定性直接举证 |

因最后一项是 Candidate 主键、错靶防护和招呼恢复定位的共同前提，不得以姓名、头像 URL、DOM 位置、列表索引或 `resumeNumber` 替代。按 M4 计划的停止条件，本批次不得假定通过并直接进入批次 1。

## 2. 已冻结的平台事实

### 2.1 当前目标不是“列表第一张卡”

- 推荐列表本身没有唯一选中语义；首卡只在本轮被用作不发送的代表性结构探针，不是产品目标裁决。
- 真人点开简历后，可见 `.new-shortcut-resume__modal` 数量为 1；该弹层和内部唯一公开动作表面可定义“当前候选人”。
- 正式 `candidate.readCurrent` 必须要求详情弹层唯一；零个或多个都失败，不滚动、不选首卡、不自动打开别人。

### 2.2 职位引用的权威组合

- URL `jobNumber` = route query `jobNumber` = talent active job `jobNumber`；轻刷新后三者继续相等。
- 可见 active job title 与 talent active job title 字面归一后相等；标题只是展示和绑定校验，不作身份。
- candidate search-condition active job 在当前页面与上述三者不相等，是可达的陈旧状态，不得用作 `positionRef` 或动作授权。

### 2.3 招呼动作是两步拓扑

1. 当前详情内唯一“打招呼”是 `button[type=button]`。
2. 第一击只打开唯一 `.ai-greeting-modal`，弹层内有两个 `.ai-greeting-modal__option`。
3. 无 AI icon 的选项可独立选中，其 edit icon 展开唯一 textarea；正文可精确写入和读回。
4. footer 只有一个最终“发送”标准按钮；该按钮才是候选人可见的不可逆动作。
5. “设置为默认，不再弹出”是独立复选项；M4 不点它，不改账号级设置。

本轮只点击第一击和弹层内的选项/编辑/关闭控件，从未点击最终“发送”。关闭后当前详情仍显示“打招呼”；复开后默认复选项仍未选中，探针正文不存在。

### 2.4 既有会话的候选人与招呼正证

- IM URL session 只匹配一条 session 记录；该记录的 `userId`、`typeUserId`、`peerPartnerId` 归一后相等。
- 同一会话的简历详情 candidate userId 与 session userId 相等，jobNumber 也相等；这说明建联后可以用平台 userId + 职位引用确认唯一会话。
- 历史招呼行的 raw `type` 是字符串 `custom`，数字 `131` 在 content envelope 的 `type`；正文位于 `greetingText`。平台文档不得误写成“raw type = 131”。
- 该行有稳定 `idServer`、`status=success`且 `from` 为当前 staff；这四项叠加正文 hash 才能作为已发招呼正证。
- timelineMap 的键与容器 `sessionId` 等于 `conversationRef`；消息行自身的 `sessionId` 字段在当前 9 条样本中全部不等于 conversationRef，不得用作会话绑定证据。
- 轻刷新前后，conversation、peer、job、招呼 `idServer` 与正文的脱敏指纹全部相等；本文不保存这些指纹值。

## 3. 唯一阻塞项：新候选人的发送前身份

真机公开页面事实如下：

- 当前详情弹层、对应候选人卡、URL 与动态后 outerHTML 均没有 `userMasterId/userId`；
- 页面初始化 JSON 含职位和登录用户状态，但不含当前未建联候选人 userId；
- 姓名、头像 URL、可见顺序与列表 `data-index` 均不是平台 userId；
- 旧项目代码从 `listitem.__vue__._props.source.userMasterId` 取值，但新项目的浏览器事实探针位于隔离 world，本轮只能确认线索存在，不能确认当前值或轻刷新稳定性。

这不是“页面一定没有平台 userId”的结论，而是“本轮还没有符合文档信任边界的真机正证”。在正证出现前：

1. `candidate.readCurrent` 不得约定为已可实现的 capability；
2. 不得用 `resumeNumber`、姓名、头像或 DOM 位置降级；
3. 不得进入任何 effectful 招呼实现或真机发送。

建议下一步是单独裁决一次**不发送、不落盘原值、不进 contract/capability 的 MAIN-world 身份事实探针**，只输出三个布尔结论：

- 唯一当前详情是否唯一绑定一个非空 `userMasterId/userId`；
- 关闭详情并重新打开同一候选人后，身份脱敏指纹是否相等；
- 轻刷新并由真人重新打开同一候选人后，身份脱敏指纹是否相等。

该探针的执行形态和临时代码是否允许进工作区，属于本批次外的新裁决；本文不以“诊断需要”自我授权新机制。

## 4. M3 匿名历史债务的有界恢复

按 M4 计划只执行了一次有界检索：

- 仓库当前文档只有 `docs/里程碑3-验收报告.md` 一处写了“死代码三件套与 witness 两缺陷”；
- AGENTS.md、契约、协议设计、client、plugin、scripts 中没有五项实名、文件/符号、事件序列或测试要求；
- 全分支 commit message 与最近 20 个提交也没有可恢复记录。

结论：五项债务当前是**历史记录不可执行**，不猜测、不修复、不阻塞 M4。请甲方撤销该匿名债务，或补充五项原始审查证据后再独立立项。

## 5. 本轮外部副作用核对

- 真实候选人可见招呼：**0**；
- 最终“发送”点击：**0**；
- 候选人/Profile/effect intent/conversation/message 建档：**0**；
- 平台默认招呼设置改动：**0**；
- 未发草稿留存：**0**；
- 验证后浏览器控制已释放，临时打开的推荐页已关闭，用户原有 IM 页保留。

## 6. 待甲方裁决

1. 是否批准第 3 节的一次性 MAIN-world 脱敏身份事实探针；批准前不进批次 1。
2. 是否撤销 M3 “死代码三件套与 witness 两缺陷”匿名债务；若不撤销，请补原始证据。

除上述两项外，批次 0 没有新增守卫、通用化机制、契约字段或生产代码的授权请求。
