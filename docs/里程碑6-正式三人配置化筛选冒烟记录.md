# 里程碑 6：正式三人配置化筛选冒烟记录

日期：2026-07-22

## 范围

本轮只验证一个已完成、已统一评分的正式采集批次：从该批冻结的旧后台配置读取最低分、目标人数范围和男性比例上限，以确定性代码一次形成全员筛选决策、稳定入选名单和 `selected` 候选人档案。

批次：`sb-36ce0a47b687d65640fe6374`；配置修订：`cfbba4f16c895ea065cd1d3047a6ecd2941d93cddf14c698a35cbb720bb9752c`。本轮不调用 AI provider、不调用手端、不生成招呼语或 greeting head，也不创建或派发任何 effectful 意图。

## 真实执行结果

| 检查项 | 结果 |
|---|---|
| 候选池 / 决策数 | 3 / 3 |
| 后台最低分 | 5 |
| 后台目标人数范围 | 80～90 |
| 稳定目标人数 | 87 |
| 男性比例上限 / 男性人数上限 | 50% / 43 |
| 合格 / 入选 | 1 / 1 |
| 男性入选 / 未知性别 | 1 / 0 |
| `selected` | 1 |
| `contactStateRejected` | 1 |
| `scoreBelowThreshold` | 1 |
| 其他 outcome | 0 |
| 本批新增 CandidateProfile | 1 |
| 新评分或通用 AI invocation | 0 |
| 新 greeting head | 0 |
| 新 EffectIntent / effectful command | 0 / 0 |
| 候选人正文、姓名、逐人分数或身份进入本文 | 无 |

目标人数由 `selection-target-v1` 对批次与配置修订做稳定散列得到。真实池只有 3 人，因此按“合格人数不足时选当前全部合格者”的既定语义收口，不降低最低分，也不为凑满目标补选已建联或低分候选人。

## 原子性、重放与边界核对

1. 执行前，本批筛选摘要、逐人决策和本批入选档案均为 0；执行后分别为 1、3、1。
2. 对同一 `batchId` 立即重复调用，返回的目标人数、聚合计数和 `completedAt` 完全不变；摘要仍为 1 行、决策仍为 3 行、档案没有重复创建。
3. 执行前后，`ai_invocations`、`sourcing_score_invocations`、`candidate_greeting_heads`、`effect_intents` 与 effectful `cmd_records` 的全库计数均未增加。
4. 自动化回归覆盖批次未完成、评分缺失或未终局、跨 revision/content、同分全序、人数与男性上限、已有档案、事务中途失败全回滚和完整结果重放。
5. 独立阻断级审查未发现部分名单提交、重算名单、跨批取数、管理投影泄漏或进入 provider/手端/发送路径的问题。

## 门禁与结论

- `go test ./...`：通过。
- `go run ./contract/codegen -check`：通过；本阶段没有修改脑手契约。
- 真实三人配置化筛选冒烟：通过。

“已完成正式批次 → 全员评分终局 → 读取冻结后台筛选配置 → 原子形成稳定名单”的竖切已经变绿。下一阶段是只对本批 `selected` 档案批量调用后台招呼语提示词生成个性化正文；在正文生成完成之前仍不发送。
