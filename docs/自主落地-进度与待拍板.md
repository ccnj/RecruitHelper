# 自主落地 · 进度与待拍板清单

> 甲方离开期间,由 AI 自主推进里程碑 1(脑手最小闭环)。本文是回来后的第一入口:
> 记录每步进度、验证方式,以及任何"规格未覆盖/实测冲突"而被我保守处理、需你拍板的事项。
> 纪律:不擅自改协议规格与 CLAUDE.md 的裁决;拿不准的进"待拍板",按保守默认继续。

## 进度看板

| 步骤 | 状态 | 提交 | 验证方式 |
|---|---|---|---|
| 0 建仓 | 完成 | cff9203 | git 历史 |
| 1 契约 codegen | 完成 | e18d050 | -check 一致 + tsc strict |
| 2.1 服务骨架与存储 | 完成 | 882604e | go test + Win 交叉编译 |
| 2.2 握手与配对 | 完成 | 5227e71 | 5 集成测试 + CLI 剧本 |
| 2.3 心跳与手注册表 | 完成 | 01ce2a6 | 单元+集成+race + kill -9 演示 |
| 2.4 派发器 happy path | 完成 | 8735621 | 派发/去重测试 + 三类命令 CLI |
| 2.5a 超时引擎与 suspect 基础 | 完成 | 37d2644 | dispatch 单元测试 7 组 + race |
| 2.5b 重连收编+重启扫描+人裁 | 完成 | (本次) | dispatch 单元 13 组 + CLI 收编/裁决/ackTimeout 演示 |
| 2.5 对抗审查 | 完成 | (本次) | 5 维度多代理红队,24 提出→14 确证 |
| 2.5 审查修订 | 完成 | 3485931 | 14 findings 全修 + 回归测试 |
| 2.5 二轮验证+残漏修订 | 完成 | (本次) | 4 提出→2 确证(均次要)全修;无死锁/新致命 |
| 2.6 帧观测台 SSE | 完成 | 7f78589 | curl SSE 冒烟捕获全生命周期 |
| 2.6 剧本固化回归测试 | 待办(下一步) | — | — |
| 3 真 Chrome 插件 | 待办 | — | — |
| 3 真 Chrome 插件(base+program) | 待办 | — | — |
| 4 最小 UI(状态页+测试页) | 待办 | — | — |
| 5 验收 §16 + Electron 壳 | 待办 | — | — |

## 待你拍板清单

（暂无。出现规格未覆盖/实测冲突时追加,每条含:现象、我采取的保守默认、影响面、备选。）

## 已归档:2.5 对抗审查发现（14 确证,全修复）

多代理红队(5 维度)对 2.5 故障轨道审查,提出 24 条、确证 14 条(2 致命 7 重要 5 次要),已逐条修复+回归测试:
- **致命 F1**:并发 MutateCmd 撞 SQLITE_BUSY 被静默吞→丢结果→假 suspect→双发。红队真机复现(194/200 失败)。修:`SetMaxOpenConns(1)` 串行化写;去掉 no-op 的 FOR UPDATE。回归测试 `TestConcurrentMutateNoBusyNoLost`。
- **致命 F10**:OnResult 先写去重表后终局化,非原子→终局化失败则 result 永久吞。修:终局化不再 gate 于去重表(幂等,重传补上),去重表仅作观测。
- **重要 F4**:重派路径不查冻结域→intrusive 被派进冻结域(违法条4)。修:重派前复查 HasSuspectInDomain。
- **重要 F5/F8**:瞬态拒绝回 queued 后无重投→滞留到 deadline 误 suspect。修:sweep 对 queued 在线命令重投驱动。
- **重要 F7**:suspect 收 possible 迟到 result 被抹平为 failed(违"歧义只转人工")。修:possible/confirmed 保持 suspect。
- **重要 F11**:sweep 逐命令关连接+wedged→真实异步关连接下 wedged 暴涨误报。修:每手每 sweep 至多关一次。
- 次要 F2/F3/F6/F9 + 测试盲区 F12/F13/F14 全部修复并补测。
- 详细报告存 `/private/tmp/.../tasks/w5tfqnfj0.output`(会话临时区,过期即失)。

## 我留给你的两条尾巴（已知,非待拍板）

1. 最终人眼体感验收(自动化能确认行为发生,产品体感要你亲点)。
2. Windows 真机与安装包(Go 交叉编译每步都验,真机运行留到你回来)。

## 关键约束速查（防我自己跑偏）

- 主动权在脑;业务节奏只在脑,手无业务定时器。
- 全新起盘,不背旧契约;平台无关(DOM 只进 evidence/日志)。
- 测试=生产同一通道;宁可少发,歧义只转人工不自动重发。
- 表结构骨架期临时,首个发布前换 append-only migration。
