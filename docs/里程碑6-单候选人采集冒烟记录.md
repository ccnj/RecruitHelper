# 里程碑 6：单候选人采集冒烟记录

日期：2026-07-22

## 范围

本次只验证“脑发起一次推荐页简历读取，手打开当前推荐卡片详情并回传，脑持久化不可变采集事实”。没有评分、建档、招呼或其他候选人可见副作用。

职位上下文暂由已登录旧后台管理页的当前线上版本导出可见文档后，以 `localImport` 部分快照导入；旧客户端 `/client/job-config` 凭证当前返回 401，因此本记录不声称正式旧后台配置接口已经接通。导入包只含当次冒烟所需且能从管理页逐项核实的 6 类文档，未伪装成完整活动版本。

## 真机序列

1. Chrome 中打开智联推荐页，URL 带当前职位参数，推荐列表可见。
2. 重载后的手与脑契约一致，手声明 `candidate.readSourcingResume@1`。
3. 脑发现身份 boot/session 已过期，先派发 `probe.platform`；主体指纹一致后更新当前身份代次。
4. 脑派发一次 `candidate.readSourcingResume`；手在推荐页打开当前卡片详情、读取简历并返回正结果。
5. 脑按 logical dispatch 去重并新增一条 `SourcingCandidateRun`，随后管理面只返回引用、摘要、字节数和时刻。

## 结果

| 检查项 | 结果 |
|---|---|
| 采集命令终局 | `ok` |
| 新增采集事实 | 1 条 |
| 简历 schema | v1 |
| 简历正文大小 | 2422 bytes |
| 内容 SHA-256 | `b4a1ad024a8d65c2df6194105af4c4039ba021a3901e701fb646e4eac8f8f302` |
| 管理响应候选人明文 | 无 |
| 候选人可见副作用 | 无 |

## 结论与后续

“推荐页单候选人 → 脑侧不可变采集事实”真实生产路径已绿。正式旧后台 job-config 接口仍作为配置平面后续项，取得有效客户端凭证后替换当前 `localImport` 部分快照。

## 评分追加冒烟

同日以提交 `d4634d3` 重启脑后，actor 优先拾取上述未评分采集事实，没有再次采集候选人；它在持久化唯一评分预留后调用已配置的 DeepSeek V4 Pro 一次，并把合法整数分数与计量原子收编。

| 检查项 | 结果 |
|---|---|
| provider 物理调用 | 1 次 |
| 终局 | `ok` |
| 分数 | 7 |
| 输入 / 输出 token | 1565 / 244 |
| cached input token | 0 |
| 非思考用量闸 | 通过 |
| 估算费用 | 893 micro-USD |
| 管理响应候选人明文 | 无 |

“采集事实 → 一次评分事实”真实生产路径已绿。该分数达到当前默认选中阈值，下一独立因果批次进入合格建档与自动招呼；发送仍必须复用既有 WAL / idemKey / 正证轨道。

## 自动选人与招呼追加冒烟

同日以提交 `46320b7` 接入合格建档与自动招呼，并以 `1b64469` 修复推荐列表被单个内容不完整候选人持续阻塞的问题。插件通过管理端自重载完成换代，bootId 已变化且 contractHash 与脑一致，无需人工刷新扩展。

真实推荐页随后取得一份完整简历，调用配置中的 DeepSeek V4 Pro 评分为 8 分，高于当前阈值 5。脑在同一事务中写入 `selected` 决策、候选人根与职位档案，随后仅调用既有 `Dispatcher.SendGreeting`，经 EffectIntent / Cmd WAL 派发一条固定正文“你好”。

| 检查项 | 结果 |
|---|---|
| 采集命令终局 | `ok` |
| 评分终局 | `ok`，8 分 |
| 输入 / cached input / 输出 token | 1211 / 896 / 212 |
| 估算费用 | 325 micro-USD |
| 选人结果 | `selected`，阈值 5 |
| effectful 入口 | 既有 `chat.sendGreeting` WAL 路径 |
| 平台可见消息 | 唯一一条“你好”消息气泡 |
| 同候选人重复发送 | 0 |
| 最终命令 / intent 状态 | `ok` / `ok` |
| 管理响应与本文候选人明文 | 无 |

首次发送后的三轮自动 `chat.readGreetingOutcome` 均在推荐页的旧会话视图中返回阴性，因此系统按既有内核进入 `suspect`，没有补发。有人值守检查只观察到一条消息气泡；打开平台“继续沟通”所创建的 IM 页后，通过既有 `resolvedOk` 人工裁决入口触发一次正式验证读，机器取得唯一候选人、职位、会话与服务端招呼正证，原命令和 intent 原子收敛为 `ok`。

因此，本次已证明“采集 → 评分 → 自动选人 → WAL 唯一发送 → 正证落账”真实路径可达，但正证读取仍依赖 IM 感知面已经存在。下一独立修复只负责在招呼验证读前自动准备 IM 感知面，不改变发送动作、重试纪律或成功判据。

## 推荐页关系状态正证追加冒烟

同日将招呼成功的最小正证改为：同一推荐页详情中，已绑定的候选人与职位不变，唯一可见动作由“打招呼”变为“继续沟通”。一次明确读取即构成平台关系已建立的正证；页面无变化、目标丢失或读取失败仍只是“未确认”，不得补发。

硬切换后使用真实推荐页连续采集评分：前两份新样本低于阈值，均未触发消息；第三份新样本评分 9，高于阈值 5，触发一次既有 `chat.sendGreeting` WAL 路径。页面在最终动作后立即返回同一目标的“继续沟通”，手返回正结果，脑原子收敛命令、intent 与候选人档案。

首次正路径绿后、账号暂停命令实际生效前，定时 actor 又处理了一份 6 分样本，对另一名候选人同样只调用一次“打招呼”并取得同页正证。这超出“总共只测一条”的预期，但没有对同一候选人重复发送；本记录按实际两条计数，不隐去第二条。

| 检查项 | 结果 |
|---|---|
| 本轮新样本评分 | 1、1、9、6 |
| 选人阈值 | 5 |
| 候选人可见动作 | 两个不同目标各唯一一次“打招呼” |
| 推荐页正证 | 两个目标均读到同目标“继续沟通” |
| 命令 / intent 终局 | 2 组 `ok` / `ok` |
| 候选人档案 | 2 条 `greeted` |
| 会话引用 | 2 条均为 `NULL`，等待后续巡检绑定 |
| 会话与首条消息伪造 | 0 |
| 验收前后 IM 标签 | 唯一既有标签身份不变，无新建、刷新或关闭 |
| 同候选人补发 | 0 |

验收收尾时已暂停账号巡检并停止脑进程，不会在提交审查期间继续产生测试消息。结论：“采集 → 评分 → 选人 → WAL 每目标唯一发送 → 原推荐页关系正证 → 待巡检绑定”的最窄真实路径已绿，不再为每次招呼开关 IM 页。
