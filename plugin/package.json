{
  "name": "recruithelper-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "招聘助手手端(Chrome MV3):base 稳定壳 + program 原语。脑手协议见 ../contract。",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test:node": "node test/run.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "ws": "^8.21.1"
  }
}
