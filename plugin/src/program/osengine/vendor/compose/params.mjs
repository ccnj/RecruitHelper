// 排版参数。默认值由真人基线拟合而来，**不是标定结果** —— 标定是第 5 轮的事。
//
// 拟合自 lab/calibrate/baseline/human-2026-08-20.json（macOS）：
//   段内按键间隔  中位 192ms  CV 0.642   对数正态 median 217 σ 0.513
//   段间间隔      中位 709ms  CV 0.741   对数正态 median 761 σ 0.834
//   dwell         中位  87ms  CV 0.628   IQR/μ 0.376  ← 重尾，需混合分布
//
// dwell 用两成分：单个对数正态的 CV 与 IQR/μ 被 σ 绑死，要 CV=0.628 就得 σ≈0.576，
// 那时 IQR/μ 会到 0.675，中段比真人散得多。主体 + 偶发长按才贴得上两个矩。
//
// ── Windows 真人基线（human-win-2026-08-21.json，Chrome 151 + 微软拼音）──
//
//   dwell  中位 97ms  CV 0.322  IQR/μ 0.323  → quorble 0.354
//
// 与 macOS 差得很远：CV 只有一半，分布是紧的单峰（p5=57 / p95=143）而不是重尾。
// **但离散度刻意不往 Windows refit。** 理由：判据是单边的（quorble > 0.6 才命中），
// Windows 真人本身就在 0.354、离阈值只剩 0.246，而且短消息上会真的越线 ——
// 实测真人 dwell 在 n=20 窗口上有 11.4%、n=40 窗口上有 1.6% 的概率越过 0.6。
// 照着 refit 等于主动把自己挪到一个真人都站不稳的位置。
//
// 比真人更散不可疑：macOS 真人本来就是 0，说明 0 完全在人类分布之内。
// 所以只改**位置**（合成 dwell 中位 87 → 97），不改形状。成分中位 78/190 → 93/226
// 是标定出来的，不是等比换算：混合分布的整体中位不等于成分中位的线性缩放，
// 而且 keydurations 里还混着 Shift 的长按（中位 330/680），必须实测回归。
// 这一改对判据零影响（CV 与 IQR/μ 都是无量纲的，纯缩放恒等），只是更贴 Windows 的手感。

export const DEFAULT_PARAMS = {
  /** 每键按压时长（keydurations） */
  dwell: {
    mix: [
      { w: 0.88, median: 93, sigma: 0.30 },
      { w: 0.12, median: 226, sigma: 0.50 },
    ],
  },

  /** composition 期间相邻拼音键的 keydown 间隔 */
  intraKey: { mix: [{ w: 1, median: 105, sigma: 0.50 }] },

  /** 上屏键（Space/数字）相对最后一个拼音键的间隔 —— 真人选词前有一次确认停顿 */
  commitKey: { mix: [{ w: 1, median: 185, sigma: 0.55 }] },

  /** 上屏 → 下一段首键 */
  interSeg: {
    mix: [
      { w: 0.84, median: 520, sigma: 0.62 },
      { w: 0.16, median: 1500, sigma: 0.42 },
    ],
  },

  /** 直接上屏字元（标点/数字/换行）前后的间隔 */
  directGap: { mix: [{ w: 1, median: 330, sigma: 0.60 }] },

  /**
   * Shift 自身的时序。**它是一个会被完整统计的普通键** —— 有 down 有 up，
   * pairKeys 正常配对，于是它的 dwell 进 keydurations、startTime 进
   * keyboardRhythm、"ShiftLeft" 进 key 序列参与 find_dominant_char 占比。
   *
   * 早先把它当辅助操作、用固定偏移排（lead 35ms、up = 被修饰键.up + 25），
   * 有两个毛病：
   *   1. lead 固定 → keyboardRhythm 里出现精确重复的 35；
   *   2. Shift.dwell ≡ 被修饰键.dwell + 60 —— 一条完美的线性关系，
   *      是「过于规整」的教科书形态。
   * 真人那 4 次里两者毫无固定关系。故 lead 与 dwell 各自独立采样。
   *
   * 参数量级取自第一份真人基线（n=4，够定量级不够定形状，待补样本）：
   *   lead  70 / 103 / 149 / 672 ms
   *   dwell 275 / 368 / 480 / 887 ms
   * 真人按 Shift 是「压住 → 找键 → 按 → 松 → 再松 Shift」，中间有寻键时间，
   * 所以比直觉长得多。
   */
  shift: {
    lead: { mix: [{ w: 0.78, median: 105, sigma: 0.45 }, { w: 0.22, median: 380, sigma: 0.55 }] },
    dwell: { mix: [{ w: 0.72, median: 330, sigma: 0.38 }, { w: 0.28, median: 680, sigma: 0.42 }] },
    /** Shift 的 up 必须盖过被修饰键的 up，再留这么多余量 */
    coverMs: 30,
    /** 进入带 Shift 的键时，gap 至少比 lead 多这么多 —— 否则 Shift 会早于上一个键按下 */
    entryMarginMs: 30,
  },

  /** 上屏键的选择。真人基线 Space 占全部按键 0.125，未见明显数字选词 */
  commitKeys: [
    { code: 'Space', p: 0.86 },
    { code: 'Digit2', p: 0.06 },
    { code: 'Digit3', p: 0.05 },
    { code: 'Digit4', p: 0.03 },
  ],

  /** 硬约束 */
  limits: {
    /** speed = 总耗时 / textLen 必须 ≥ 此值（判据 33006/33007/33008 的最宽档是 100） */
    minSpeedMsPerChar: 130,
    /** 任一相邻输入间隔超过它就会触发 5 秒 debounce，把一次输入切成两次 report_typing */
    debounceMs: 5000,
    /** 留出余量，避免采样尾部意外越过 debounce */
    maxGapMs: 4200,

    /**
     * 同一个物理键：上一次松手到下一次按下必须留够这么久。
     *
     * **这不是「更像真人」，是物理约束** —— 你按不下一个正按着的键。
     * 违反它的后果不是不像，是 Windows 把第二次 keydown 标成 `repeat: true`，
     * 而 pairKeys 跳过 repeat 记录、永不配对，于是那一对凭空消失：
     * 实测 plan3 的 97 键里有 3 处自重叠，真机就报了 3 个 repeat=true，
     * 离线模型比真机多算 1 对（另两处因配对顺序变化恰好抵消）。
     *
     * 真人实测（human-win-2026-08-21，376 个样本）：最小 76ms，零个负值，
     * 50ms 以下一个都没有。最小的 8 个是 76/81/83/88/105/108/110/112，
     * 之后**直接跳到 168** —— 121 落在这个天然空档里，不从密集区中间切。
     *
     * **取 121 而不是实测最小值 76，是保守，不是必需 —— 这一条改过一次。**
     *
     * 当初的理由是「注入抖动会吃掉余量」：keydown 漂移中位 −3ms（最大 11）、
     * dwell 差 −14~+15ms，估计最坏吃掉约 26ms。**这个估计后来被真机推翻了。**
     * 第二轮四份注入实测，计划最小余量 222/132/125/142ms，真机观测
     * 222/132/124/142ms —— **实际只吃掉 0/0/1/0ms**。
     *
     * 原因是当初把两项误差当独立的最坏情况相加了：整份计划跑在同一个时钟上，
     * 按下与松开一起漂移，**相对间距被保留**，绝对漂移互相抵消。
     * 按这个实测，76ms 同样安全。
     *
     * 仍然保留 121 的理由只剩「留冗余」：上述实测是安静系统下四份计划的结果，
     * 系统负载重时（远控软件在抓屏、别的进程抬时钟）抖动可能更大，而代价只是
     * 禁掉真人 2.1%（8/376）的左尾行为、重采中位从 1~2 升到 1~3。
     * 若哪天需要更贴真人，降回 76 有实测依据支持。
     *
     * 代价：禁掉真人 2.1%（8/376）的左尾行为，重采次数中位从 1~2 升到 1~3。
     * 都可忽略。150ms 就开始吃力了（长文案重采最大 26 次，接近 maxTries）。
     *
     * 另一个角度的互证（对 76 成立，留作记录）：真人同键隔 2 个键复现
     * （如 n-i-n）的 down→down 最小值是 170ms，而 dwell 中位 97 + 76 = 173ms。
     *
     * 真人满足这条靠的是**拉长间隔**而不是缩短 dwell：全部相邻按键的
     * down→down 最小 19ms，而同键复现隔 1/2/3 个键的最小值是 139/170/254ms。
     * 所以修法是拒绝重采（得到的正是受约束的条件分布），不是钳制 ——
     * 钳制会在 guard 下限上堆出一批精确重复值，Shift 那条约束已经吃过这个亏。
     */
    sameKeyGuardMs: 121,
    /** 单个物理键码占比上限。主分支 find_dominant_char 阈值 0.7，chatJob 的 v2 是 0.5 */
    maxKeyShare: 0.45,
    /** 四条序列的 quorble 上限。0 才是真人形态；>0.6 直接命中 flimbot */
    maxQuorble: 0,
    /** 最短按压，低于此值不真实 */
    minDwellMs: 28,

    /**
     * Shift 松手后到下一个键按下的最小余量。**安全下限，不是时序量**：
     * 正常情况下由 shift.* 的采样自然拉开，用不到它；一旦被钳到就说明
     * 排版排得太挤，该重采（localCheck 会拦）。
     *
     * 少了这道会怎样：2026-08-21 实测「？」之后的 x 在 Shift 仍按住时按下，
     * 输入法把大写 X 当英文，「薪资」变成「Xin子」。
     */
    shiftGuardMs: 40,
    /** 被 Shift 修饰的那个键，其按压时长上限 —— 它越长，Shift 要盖的时间越长 */
    shiftMaxDwellMs: 110,
  },
}

export function withParams(over = {}) {
  return {
    ...DEFAULT_PARAMS,
    ...over,
    limits: { ...DEFAULT_PARAMS.limits, ...(over.limits || {}) },
  }
}
