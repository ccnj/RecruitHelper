// 阳性对照载荷(2026-08-28,临时)。
//
// 故意在页面上留一个**可枚举**的 window 全局。BOSS 的 800001「device-action-report」
// 会把 Object.keys(window) 与自己那份 445 项白名单的差集原样放进 p6 上送。
// 如果这个名字出现在 p6 里,就同时证明三件事:
//   1. 他们那套未知全局检测确实在跑
//   2. 判据与我们理解的一致(可枚举全局 → 进差集)
//   3. 我们的埋点观测确实抓得住这条上报
//
// 名字取得刻意好认,方便在上送载荷里一眼找到。验完删除。
;(window as unknown as Record<string, unknown>).__rhCanary_20260828 = 1
