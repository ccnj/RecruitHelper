@AGENTS.md

# Claude Code 补充(2026-07-25)

**本文件不是宪法,`AGENTS.md` 才是。** 上面一行把 AGENTS.md 整份导入,本文件只放 Claude Code 特有的行为约束,**不复制、不改写、不摘录** AGENTS.md 的任何条款。

立此文件的原因:本文件此前是 AGENTS.md 的手抄副本,自 2026-07-19 起停止同步,到 07-25 已缺失「冒烟冲刺临时剖面」「防护成本预算」「增量交付、提交与停止纪律」三整章,以及全局约定中七条甲方裁决(业务事实禁止物理 DELETE、AI provider 数据边界、AI 动作建议封闭枚举、平台交互节奏与条件等待、推荐页运行连续性、统一业务运行窗口、同机产品 UI 业务投影例外)。过时的完整副本比缺失更危险——它看起来自洽,读它的一方不会怀疑自己少了什么。此后 AGENTS.md 是唯一维护点,禁止在本文件重建副本。

## 给 Claude 的行为约束

以下两条不是新规范,是 AGENTS.md「增量交付、提交与停止纪律」第 1、12 条对本工具的定向强调——两条都已发生过实际违反。

1. **战役级实现先冻结出口,等甲方确认再动手;原子级说明意图即可直接干(2026-08-02 甲方裁决分层,分层定义见 AGENTS.md 该章第 1 条)。** 对战役级工作,甲方回答设计问题、澄清需求或在候选方案里选定一支,**都不等于开工授权**;开工授权是甲方看过出口四项后的明确同意。原子级工作完成即报,甲方随时可叫停;拿不准属哪级,按战役级处理。

2. **规格与实现禁止同批落地。** 按第 12 条,凡需要修改 `AGENTS.md`、`contract/协议规格-v1.md`、`docs/沟通逻辑规格-v4.md` 或任何已冻结事实记录——包括放宽约束、扩大范围、增加保证、新增持久化/迁移/兼容义务——必须先冻结实现,并列提交「旧约束 / 拟议文本 / 当前可达场景 / 收益与成本」交甲方裁决,获批后才按"规范先行 → 实现与生成产物同步"落地。同批新改的规范不得被引用为该实现的授权或正确性证据。

## 工具差异备注

- 终端交互式面板类斜杠命令(`/permissions`、`/config`、`/agents`、`/hooks` 等)在桌面端会话内不可用,不要建议甲方在此运行。
- 子代理与 workflow 只在甲方明确要求时使用;AGENTS.md 对审查预算与"发现不等于授权"的约束同样适用于子代理产出。
